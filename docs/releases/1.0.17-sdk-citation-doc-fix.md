# agent-search-core 1.0.17

`agent-search-core` `1.0.17` is a documentation-only patch release.

## README updates

- Removes `document_id` from the list of required citation metadata keys.
- Clarifies that the runtime falls back to `Document.id` automatically when it is available from the vector store.
- Removes the extra `page_content` note from the citation metadata section.

## Runtime behavior

- No runtime code changes relative to `1.0.16`.
