- [ ] P0 — Replace scaffold orchestration with real LangGraph execution for `/api/agents/run` (spec: `specs/orchestration-langgraph.md`). Build a compiled LangGraph graph with explicit nodes for decomposition, tool selection, retrieval, validation, and synthesis; run requests through that graph instead of a hand-wired Python sequence; preserve response contract in `schemas/agent.py`.
  Verification requirements: add/update backend smoke test(s) that confirm the request path executes a real LangGraph graph object, the logical steps execute in intended order, each subquery traverses retrieval+validation before synthesis, and final response still includes coherent synthesized output plus graph state projection.

- [ ] P0 — Implement true deep-agent subgraph behavior for per-subquery execution (spec: `specs/orchestration-langgraph.md`). Replace placeholder deep-agent metadata with actual deep agent usage for subquery retrieval/validation flow; ensure subquery handling is reusable and isolated as a nested agent/subgraph unit.
  Verification requirements: add/update backend smoke test(s) showing deep-agent-backed subquery execution runs for all produced subqueries, each subquery uses exactly one selected tool path, validation loop stops by sufficiency or stop condition, and execution metadata is observable in graph state.

- [ ] P1 — Add orchestration event surface consumable as heartbeat stream (spec linkage: `specs/orchestration-langgraph.md` acceptance on stream-consumable state, with contract from `specs/streaming-agent-heartbeat.md`). Add a backend streaming endpoint that emits ordered orchestration events (subqueries, step progress, validation transitions, completion) from graph state/events.
  Verification requirements: add backend smoke test(s) for streaming endpoint asserting clients receive subquery events during run, progress events are ordered and parseable, and a terminal completion event contains final answer payload.

- [ ] P1 — Wire frontend demo to consume orchestration heartbeat for live LangGraph/deep-agent progress (spec linkage: `specs/demo-ui-typescript.md`, `specs/streaming-agent-heartbeat.md`). Replace run-time polling/single-response dependency with stream-driven updates so subqueries and progress render during execution, then finalize with synthesized answer.
  Verification requirements: add/update frontend interaction test(s) validating subqueries appear incrementally from stream events, progress state changes while run is in-flight, and final answer appears on completion without breaking existing load/run error handling.

- [x] Completed baseline in scope — Scaffold topology and deterministic pipeline data are already present for planning reference: `src/backend/agents/langgraph_agent.py` exposes a LangGraph-like projection and timeline; `src/backend/tests/api/test_orchestration_langgraph.py` and related smoke suites cover ordered step execution, per-subquery retrieval/validation outcomes, and response structure.
  Verification status: existing tests validate scaffold behavior, but they do not yet prove execution by real LangGraph runtime or true deep-agent integration.
