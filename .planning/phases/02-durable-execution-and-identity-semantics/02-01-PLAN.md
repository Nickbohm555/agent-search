---
phase: 02-durable-execution-and-identity-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/pyproject.toml
  - src/backend/uv.lock
  - src/backend/alembic/versions/008_add_runtime_execution_durability_tables.py
  - src/backend/models/__init__.py
  - src/backend/models/runtime_execution.py
  - src/backend/agent_search/runtime/persistence.py
  - src/backend/agent_search/runtime/execution_identity.py
  - src/backend/tests/runtime/test_execution_identity.py
autonomous: true
must_haves:
  truths:
    - "Runtime has durable Postgres storage for run/thread identity and replay-related state."
    - "Thread ownership rules are explicit and deterministic for every new or resumed run."
    - "LangGraph checkpointer bootstrap can be initialized from application code without manual SQL."
  artifacts:
    - path: "src/backend/alembic/versions/008_add_runtime_execution_durability_tables.py"
      provides: "Run identity, checkpoint mapping, and idempotency ledger schema."
    - path: "src/backend/models/runtime_execution.py"
      provides: "ORM models for durable run/thread/effect records."
    - path: "src/backend/agent_search/runtime/persistence.py"
      provides: "PostgresSaver lifecycle and graph compile helper."
    - path: "src/backend/agent_search/runtime/execution_identity.py"
      provides: "Thread ID mint/validate/resolve policy."
  key_links:
    - from: "src/backend/agent_search/runtime/persistence.py"
      to: "langgraph.checkpoint.postgres.PostgresSaver"
      via: "checkpointer factory and setup() bootstrap"
      pattern: "PostgresSaver"
    - from: "src/backend/agent_search/runtime/execution_identity.py"
      to: "src/backend/models/runtime_execution.py"
      via: "persisted run_id <-> thread_id mapping contract"
      pattern: "thread_id"
---

<objective>
Establish durable persistence primitives and explicit thread identity ownership for runtime execution.

Purpose: Create the storage and runtime foundation required for REL-01 and REL-02 before wiring API/SDK flows.
Output: Postgres durability schema, runtime persistence helpers, and deterministic thread identity utilities.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-durable-execution-and-identity-semantics/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add durable runtime execution schema and models</name>
  <files>src/backend/alembic/versions/008_add_runtime_execution_durability_tables.py, src/backend/models/__init__.py, src/backend/models/runtime_execution.py</files>
  <action>Create SQLAlchemy models and Alembic migration for durable execution records: (a) run registry keyed by run_id with stable thread_id and lifecycle status, (b) checkpoint/session linkage fields needed for Postgres checkpointer lookup, and (c) side-effect idempotency ledger with unique key scope (thread_id + node_name + effect_key). Keep naming explicit and avoid generic JSON blob-only schema so retry/replay lookups are indexable.</action>
  <verify>docker compose exec backend uv run alembic upgrade head && docker compose exec db psql -U agent_user -d agent_search -c "\dt"</verify>
  <done>Migration applies cleanly and runtime durability tables exist with unique constraints supporting replay-safe lookup.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Postgres checkpointer lifecycle helper</name>
  <files>src/backend/agent_search/runtime/persistence.py</files>
  <action>Add a runtime persistence module that encapsulates checkpointer creation, first-run setup(), and graph compile helper using langgraph-checkpoint-postgres PostgresSaver. Include safe connection handling and a single entrypoint used later by runtime runner code; do not couple this helper to HTTP/router code.</action>
  <verify>docker compose exec backend uv run python -c "from agent_search.runtime.persistence import ensure_checkpointer_bootstrap; ensure_checkpointer_bootstrap()"</verify>
  <done>Runtime code can initialize PostgresSaver tables and return a ready checkpointer without manual SQL.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce deterministic thread identity policy</name>
  <files>src/backend/agent_search/runtime/execution_identity.py, src/backend/tests/runtime/test_execution_identity.py</files>
  <action>Implement identity policy utilities: accept optional client-provided thread_id, validate canonical format, mint server ID when absent, and persist/return immutable mapping per run. Use a hybrid ownership policy (client-supplied or server-minted) and reject invalid/blank IDs with clear errors.</action>
  <verify>docker compose exec backend uv run pytest tests/runtime/test_execution_identity.py</verify>
  <done>Given the same run lineage, resolved thread_id is stable and invalid identity inputs fail fast with explicit exceptions.</done>
</task>

</tasks>

<verification>
Apply migration, then exercise checkpointer bootstrap and thread identity unit tests in a clean backend container session.
</verification>

<success_criteria>
Durable execution foundation exists in Postgres, runtime can bootstrap LangGraph persistence, and thread ID ownership rules are codified and test-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/02-durable-execution-and-identity-semantics/02-01-SUMMARY.md`
</output>
