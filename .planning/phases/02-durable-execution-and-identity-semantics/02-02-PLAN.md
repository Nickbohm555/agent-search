---
phase: 02-durable-execution-and-identity-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/backend/schemas/agent.py
  - src/backend/agent_search/public_api.py
  - src/backend/routers/agent.py
  - src/backend/agent_search/runtime/jobs.py
  - src/backend/services/agent_jobs.py
  - src/backend/tests/api/test_agent_run.py
  - src/backend/tests/sdk/test_public_api_async.py
autonomous: true
must_haves:
  truths:
    - "API and SDK surfaces expose the same stable thread_id used by runtime execution."
    - "Async start/status/resume responses include run and thread identity needed for durable continuation."
    - "Run identity remains stable across transitions (queued, running, paused, resumed, completed)."
  artifacts:
    - path: "src/backend/schemas/agent.py"
      provides: "Request/response contracts carrying thread identity and resume metadata."
    - path: "src/backend/agent_search/public_api.py"
      provides: "SDK API contract enforcing thread continuity and identity propagation."
    - path: "src/backend/routers/agent.py"
      provides: "HTTP route plumbing for thread-aware runtime operations."
    - path: "src/backend/agent_search/runtime/jobs.py"
      provides: "Job state now stores and returns thread identity."
  key_links:
    - from: "src/backend/routers/agent.py"
      to: "src/backend/agent_search/public_api.py"
      via: "thread_id pass-through from HTTP payloads into SDK runtime"
      pattern: "thread_id"
    - from: "src/backend/agent_search/public_api.py"
      to: "src/backend/agent_search/runtime/jobs.py"
      via: "run_async and status payload mapping"
      pattern: "run_async|get_run_status"
---

<objective>
Make thread identity a first-class and stable contract across API, SDK, and runtime job/status surfaces.

Purpose: Satisfy REL-02 by eliminating identity drift between HTTP requests, SDK operations, and execution records.
Output: Thread-aware request/response schemas and end-to-end API/SDK propagation of thread_id.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-durable-execution-and-identity-semantics/02-RESEARCH.md
@.planning/phases/02-durable-execution-and-identity-semantics/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend runtime contracts with explicit thread identity</name>
  <files>src/backend/schemas/agent.py, src/backend/routers/agent.py</files>
  <action>Update request/response schema contracts to carry thread_id explicitly for sync and async runs (including optional client-provided thread_id and returned canonical thread_id in start/status/result payloads). Keep backward compatibility where feasible by adding fields instead of removing existing ones, and ensure FastAPI route handlers parse and forward identity fields consistently.</action>
  <verify>docker compose exec backend uv run pytest tests/api/test_agent_run.py -k "run_async or run_status or run"</verify>
  <done>HTTP API accepts optional thread_id, returns canonical thread_id, and tests validate stable identity fields in JSON responses.</done>
</task>

<task type="auto">
  <name>Task 2: Propagate thread identity through SDK runtime APIs</name>
  <files>src/backend/agent_search/public_api.py, src/backend/services/agent_jobs.py, src/backend/agent_search/runtime/jobs.py</files>
  <action>Update SDK `advanced_rag`, `run_async`, and status/recovery flows so thread_id is resolved once per run lineage and reused for job updates/resume operations. Ensure runtime job status stores thread_id and includes it in all status transitions; avoid regenerating thread IDs on retries or resubmissions tied to an existing run lineage.</action>
  <verify>docker compose exec backend uv run pytest tests/sdk/test_public_api_async.py</verify>
  <done>SDK/API and runtime job status agree on one thread_id per run lineage, with no thread_id churn during async lifecycle transitions.</done>
</task>

<task type="auto">
  <name>Task 3: Add thread identity contract regression coverage</name>
  <files>src/backend/tests/api/test_agent_run.py, src/backend/tests/sdk/test_public_api_async.py</files>
  <action>Add focused regression tests asserting that provided thread_id is preserved, server-generated thread_id is returned when absent, and status polling returns identical thread_id for the same run/job. Include a negative case for invalid client thread_id format to guarantee deterministic API error handling.</action>
  <verify>docker compose exec backend uv run pytest tests/api/test_agent_run.py tests/sdk/test_public_api_async.py</verify>
  <done>Thread identity contract is covered by tests across API and SDK layers with both happy-path and invalid-input scenarios.</done>
</task>

</tasks>

<verification>
Run API and SDK test suites that assert identity continuity from run creation through status polling and resumed execution entrypoints.
</verification>

<success_criteria>
For one logical run lineage, API responses, SDK return values, and runtime job state all show the same thread_id with no implicit regeneration.
</success_criteria>

<output>
After completion, create `.planning/phases/02-durable-execution-and-identity-semantics/02-02-SUMMARY.md`
</output>
