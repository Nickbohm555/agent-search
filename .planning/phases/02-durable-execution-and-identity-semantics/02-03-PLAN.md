---
phase: 02-durable-execution-and-identity-semantics
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/backend/agent_search/runtime/runner.py
  - src/backend/agent_search/runtime/resume.py
  - src/backend/services/idempotency_service.py
  - src/backend/agent_search/runtime/jobs.py
  - src/backend/agent_search/public_api.py
  - src/backend/routers/agent.py
  - src/backend/tests/sdk/test_sdk_async_e2e.py
  - src/backend/tests/services/test_agent_service.py
  - src/backend/tests/api/test_agent_run.py
autonomous: true
must_haves:
  truths:
    - "Interrupted execution can resume from Postgres checkpoints instead of restarting from initial decomposition."
    - "Replay/retry paths do not duplicate externally visible side effects."
    - "Pause/resume at HITL points only allows valid transitions and preserves state."
  artifacts:
    - path: "src/backend/agent_search/runtime/runner.py"
      provides: "Checkpoint-backed graph execution and resume wiring."
    - path: "src/backend/agent_search/runtime/resume.py"
      provides: "Resume/replay helpers using Command(resume=...) and persisted thread context."
    - path: "src/backend/services/idempotency_service.py"
      provides: "Durable idempotency ledger API for side-effect dedupe."
    - path: "src/backend/tests/sdk/test_sdk_async_e2e.py"
      provides: "End-to-end resilience coverage for interruption/replay/pause-resume."
  key_links:
    - from: "src/backend/agent_search/runtime/runner.py"
      to: "src/backend/agent_search/runtime/persistence.py"
      via: "graph compile with PostgresSaver checkpointer"
      pattern: "checkpointer"
    - from: "src/backend/agent_search/runtime/resume.py"
      to: "langgraph.types.Command"
      via: "resume commands at interrupt boundaries"
      pattern: "Command\\(resume="
    - from: "src/backend/services/idempotency_service.py"
      to: "src/backend/models/runtime_execution.py"
      via: "effect-key dedupe read/write"
      pattern: "effect_key"
---

<objective>
Deliver durable resume/replay semantics with replay-safe side effects and HITL pause/resume correctness.

Purpose: Complete REL-01, REL-03, and REL-04 by wiring checkpointer-backed runtime execution and enforcing idempotent recovery behavior.
Output: Checkpoint-resumable execution path, resume commands for HITL interruptions, and replay-safe idempotency guarantees validated by tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-durable-execution-and-identity-semantics/02-RESEARCH.md
@.planning/phases/02-durable-execution-and-identity-semantics/02-01-SUMMARY.md
@.planning/phases/02-durable-execution-and-identity-semantics/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire checkpointed execution and durable resume entrypoints</name>
  <files>src/backend/agent_search/runtime/runner.py, src/backend/agent_search/runtime/resume.py, src/backend/agent_search/runtime/jobs.py, src/backend/agent_search/public_api.py, src/backend/routers/agent.py</files>
  <action>Refactor runtime orchestration so execution is compiled/invoked with Postgres checkpointer and stable thread config, then add explicit resume/replay entrypoints that continue existing threads rather than starting new ones. Implement HITL resume using `Command(resume=...)` with strict state transition validation (only paused/interrupt-pending runs can resume). Ensure invalid resume transitions return deterministic API errors.</action>
  <verify>docker compose exec backend uv run pytest tests/sdk/test_sdk_async_e2e.py -k "resume or pause or interrupt"</verify>
  <done>Interrupted runs resume from persisted checkpoints with the same thread_id; invalid resume attempts are rejected without mutating run state.</done>
</task>

<task type="auto">
  <name>Task 2: Implement durable idempotency ledger for replay-safe side effects</name>
  <files>src/backend/services/idempotency_service.py, src/backend/agent_search/runtime/runner.py</files>
  <action>Enumerate externally visible side effects in runtime flow and gate each through durable idempotency keys (thread_id + node + effect_key) backed by Postgres ledger records. Isolate non-deterministic/side-effecting operations behind explicit task or node boundaries so replay reuses recorded outcomes instead of re-executing effects.</action>
  <verify>docker compose exec backend uv run pytest tests/services/test_agent_service.py -k "idempot or replay or retry"</verify>
  <done>Retry/replay of the same logical step returns previously recorded side-effect outcomes and does not duplicate external actions.</done>
</task>

<task type="auto">
  <name>Task 3: Add resilience integration coverage for REL-01..REL-04</name>
  <files>src/backend/tests/sdk/test_sdk_async_e2e.py, src/backend/tests/services/test_agent_service.py, src/backend/tests/api/test_agent_run.py</files>
  <action>Add integration-style tests covering: (a) crash/interruption then checkpoint resume, (b) thread_id consistency across API/SDK/status records, (c) replay-safe dedupe behavior, and (d) HITL pause/resume valid and invalid transition matrix. Avoid fragile timing assertions by asserting state transitions and persisted records.</action>
  <verify>docker compose exec backend uv run pytest tests/sdk/test_sdk_async_e2e.py tests/services/test_agent_service.py tests/api/test_agent_run.py</verify>
  <done>Automated tests prove all Phase 2 success criteria, including checkpoint resume, identity continuity, idempotent replay, and safe HITL pause/resume transitions.</done>
</task>

</tasks>

<verification>
Run targeted resilience suites and confirm checkpoint-backed recovery + idempotent replay behavior in backend containerized runtime.
</verification>

<success_criteria>
Execution survives interruption with checkpoint resume, thread identity remains stable, replay does not duplicate side effects, and HITL pause/resume transitions are safe and lossless.
</success_criteria>

<output>
After completion, create `.planning/phases/02-durable-execution-and-identity-semantics/02-03-SUMMARY.md`
</output>
