---
phase: 03-query-expansion-hitl-end-to-end
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/backend/agent_search/runtime/graph/builder.py
  - src/backend/agent_search/runtime/graph/execution.py
  - src/backend/agent_search/runtime/jobs.py
  - src/backend/agent_search/runtime/resume.py
  - src/backend/agent_search/runtime/lifecycle_events.py
  - src/backend/tests/api/test_agent_run.py
  - src/backend/tests/api/test_run_events_stream.py
autonomous: true
must_haves:
  truths:
    - "When query-expansion HITL is enabled for a run, execution pauses after expansion candidates are created and before retrieval executes."
    - "Approve/edit/deny decisions applied through run-resume deterministically control the expansion list used by retrieval."
    - "Skip decisions continue execution without requiring feedback text and without failing the run."
    - "Runs that omit query-expansion HITL configuration keep existing non-HITL behavior."
  artifacts:
    - path: "src/backend/agent_search/runtime/graph/builder.py"
      provides: "Checkpoint gate at expand-to-search boundary."
    - path: "src/backend/agent_search/runtime/jobs.py"
      provides: "Paused-state persistence and resume application for query-expansion checkpoints."
    - path: "src/backend/agent_search/runtime/resume.py"
      provides: "Decision validation and deterministic apply logic for approve/edit/deny/skip."
    - path: "src/backend/tests/api/test_agent_run.py"
      provides: "API behavior regression coverage for QEH-01..QEH-05."
    - path: "src/backend/tests/api/test_run_events_stream.py"
      provides: "Lifecycle/SSE paused and resumed stream coverage."
  key_links:
    - from: "src/backend/agent_search/runtime/graph/builder.py"
      to: "src/backend/agent_search/runtime/jobs.py"
      via: "checkpoint payload and pause metadata propagation"
      pattern: "query_expansions_ready|interrupt_payload|checkpoint_id"
    - from: "src/backend/agent_search/runtime/resume.py"
      to: "src/backend/schemas/agent.py"
      via: "typed query-expansion decision envelope validation"
      pattern: "approve|edit|deny|skip"
    - from: "src/backend/tests/api/test_run_events_stream.py"
      to: "src/backend/agent_search/runtime/lifecycle_events.py"
      via: "paused event projection and stream contract assertions"
      pattern: "run.paused|interrupt_payload|checkpoint_id"
---

<objective>
Implement backend runtime checkpointing and deterministic decision application for query-expansion HITL so QEH-01..QEH-05 behaviors are enforced and regression-tested.

Purpose: Complete the backend execution semantics that consume the contracts from Plan 03-01 and expose stable paused/resume lifecycle behavior for the frontend.
Output: Expand-stage checkpoint runtime path, pause metadata emission, deterministic resume logic, and backend API/SSE regression tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-query-expansion-hitl-end-to-end/03-RESEARCH.md
@.planning/phases/03-query-expansion-hitl-end-to-end/03-query-expansion-hitl-end-to-end-01-SUMMARY.md
@src/backend/agent_search/runtime/graph/builder.py
@src/backend/agent_search/runtime/jobs.py
@src/backend/agent_search/runtime/resume.py
@src/backend/tests/api/test_agent_run.py
@src/backend/tests/api/test_run_events_stream.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert expand-stage checkpoint and deterministic decision application</name>
  <files>src/backend/agent_search/runtime/graph/builder.py, src/backend/agent_search/runtime/graph/execution.py, src/backend/agent_search/runtime/jobs.py, src/backend/agent_search/runtime/resume.py, src/backend/agent_search/runtime/lifecycle_events.py</files>
  <action>Insert a single query-expansion checkpoint after expansion candidates are produced and before search consumes them. Ensure HITL-enabled initial runs reach paused state on first execution, not only through explicit resume branches. Apply decisions deterministically by stable expansion identity: approve keeps query, edit substitutes validated edited query, deny removes selected queries without requiring feedback text, and skip bypasses review to continue default execution. Persist and emit paused metadata (`interrupt_payload`, `checkpoint_id`, stage marker) so resume can validate identity and continue safely.</action>
  <verify>docker compose exec backend uv run pytest src/backend/tests/api/test_run_events_stream.py -k "paused or checkpoint"</verify>
  <done>HITL-enabled runs pause at query expansion before retrieval; resumed runs use approved/edited/filtered expansions exactly as submitted.</done>
</task>

<task type="auto">
  <name>Task 2: Add backend regression coverage for QEH-01 through QEH-05</name>
  <files>src/backend/tests/api/test_agent_run.py, src/backend/tests/api/test_run_events_stream.py</files>
  <action>Add or extend tests to cover: per-run enablement (QEH-01), approve resume behavior (QEH-02), edit behavior before retrieval (QEH-03), deny omission without mandatory feedback (QEH-04), and skip/default continuation semantics (QEH-05). Assert paused payload includes actionable expansion candidates plus checkpoint identity, and assert non-HITL runs do not introduce new pause requirements.</action>
  <verify>docker compose exec backend uv run pytest src/backend/tests/api/test_agent_run.py src/backend/tests/api/test_run_events_stream.py</verify>
  <done>Automated API and SSE tests prove all QEH requirements and default compatibility behavior at backend boundary.</done>
</task>

</tasks>

<verification>
Run backend API/SSE tests that cover run-async, run-resume, and lifecycle events. Confirm pause occurs before retrieval in HITL mode, decisions alter retrieval inputs deterministically, and omitted HITL config preserves non-HITL behavior.
</verification>

<success_criteria>
QEH-01..QEH-05 are satisfied at backend runtime boundaries: query-expansion HITL pauses at the correct stage, approve/edit/deny/skip behavior is deterministic, and default behavior remains unchanged when HITL is omitted.
</success_criteria>

<output>
After completion, create `.planning/phases/03-query-expansion-hitl-end-to-end/03-query-expansion-hitl-end-to-end-02-SUMMARY.md`
</output>
