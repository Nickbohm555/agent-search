---
phase: 03-query-expansion-hitl-end-to-end
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - src/frontend/src/utils/api.ts
  - src/frontend/src/App.tsx
  - src/frontend/src/App.test.tsx
autonomous: true
must_haves:
  truths:
    - "When a run pauses for query-expansion HITL, the frontend presents actionable review controls instead of a failure state."
    - "Users can approve, edit, deny, or skip query expansions and resume the same paused run."
    - "After resume, the UI returns to active stream handling and reaches completion using the resumed run lifecycle."
    - "Non-HITL runs keep existing interaction flow with no required review step."
  artifacts:
    - path: "src/frontend/src/utils/api.ts"
      provides: "Typed frontend contracts and request builders for query-expansion paused payloads and resume decisions."
    - path: "src/frontend/src/App.tsx"
      provides: "Paused query-expansion review UX with approve/edit/deny/skip actions and resume trigger."
    - path: "src/frontend/src/App.test.tsx"
      provides: "Regression tests for paused-review behavior and default non-HITL continuity."
  key_links:
    - from: "src/frontend/src/App.tsx"
      to: "src/frontend/src/utils/api.ts"
      via: "resume submission handler with checkpoint-bound payload"
      pattern: "run-resume|checkpoint_id|decisions"
    - from: "src/frontend/src/utils/api.ts"
      to: "/api/agents/run-events/{job_id}"
      via: "typed paused event payload parsing"
      pattern: "run.paused|interrupt_payload"
    - from: "src/frontend/src/App.test.tsx"
      to: "src/frontend/src/App.tsx"
      via: "paused interaction and resume-flow assertions"
      pattern: "approve|edit|deny|skip"
---

<objective>
Implement the frontend query-expansion HITL review and resume workflow so paused runs are actionable and users can control retrieval-bound expansions.

Purpose: Deliver the user-facing execution path for QEH-02..QEH-05 using backend checkpoint payloads finalized in Plan 03-02.
Output: Typed frontend API models, paused review UI state machine, and integration-style tests for decision submission and continuation.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-query-expansion-hitl-end-to-end/03-RESEARCH.md
@.planning/phases/03-query-expansion-hitl-end-to-end/03-query-expansion-hitl-end-to-end-02-SUMMARY.md
@src/frontend/src/utils/api.ts
@src/frontend/src/App.tsx
@src/frontend/src/App.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed frontend query-expansion HITL payload handling</name>
  <files>src/frontend/src/utils/api.ts</files>
  <action>Extend API request/response typings and guards to support query-expansion HITL run configuration plus paused payload data (`checkpoint_id`, expansion candidates, lane identity) and typed resume decisions. Keep parsing strict enough to reject malformed payloads while preserving additive compatibility for non-HITL runs and existing event shapes.</action>
  <verify>docker compose exec frontend npm run typecheck</verify>
  <done>Frontend API layer can safely parse query-expansion paused payloads and build typed approve/edit/deny/skip resume requests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement query-expansion paused review and resume UX</name>
  <files>src/frontend/src/App.tsx</files>
  <action>Add paused-review UI state for query-expansion checkpoints triggered by `run.paused` lifecycle events. Render per-expansion controls for approve, edit, and deny; include skip behavior that resumes without item-level edits. Bind resume submission to `job_id` and `checkpoint_id`, then transition the app back to active streaming until terminal completion. Ensure paused status is treated as an expected actionable state, not as run failure.</action>
  <verify>docker compose exec frontend npm run test -- App.test.tsx</verify>
  <done>Users can review expansions, submit decisions, and see the run continue and complete from the same paused checkpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Add frontend regression tests for QEH paused/non-paused flows</name>
  <files>src/frontend/src/App.test.tsx</files>
  <action>Update integration-style tests to validate: paused query-expansion payload renders review controls, approve/edit/deny/skip interactions generate expected resume payload shape, resumed stream transitions correctly, and runs without query-expansion HITL continue through existing UI flow with no review panel shown.</action>
  <verify>docker compose exec frontend npm run test -- App.test.tsx && docker compose exec frontend npm run typecheck</verify>
  <done>Frontend tests lock in actionable paused behavior and non-HITL backward compatibility.</done>
</task>

</tasks>

<verification>
Run frontend tests and typecheck for the app flow. Confirm paused query-expansion runs render review controls, decision submissions call run-resume correctly, and non-HITL runs preserve prior UX behavior.
</verification>

<success_criteria>
Query-expansion HITL is usable in the app: pause is actionable, approve/edit/deny/skip actions are supported end-to-end, and legacy non-HITL user flow remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/03-query-expansion-hitl-end-to-end/03-query-expansion-hitl-end-to-end-03-SUMMARY.md`
</output>
