---
phase: 03-query-expansion-hitl-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/schemas/agent.py
  - src/backend/routers/agent.py
  - src/backend/agent_search/public_api.py
  - src/backend/tests/api/test_agent_run.py
  - sdk/core/src/schemas/agent.py
autonomous: true
must_haves:
  truths:
    - "Run-async accepts additive query-expansion HITL enablement fields without breaking legacy payloads."
    - "Run-resume accepts typed decision envelopes for query-expansion checkpoints."
    - "Backend and SDK request/response models stay schema-compatible for the new HITL fields."
    - "Existing non-HITL runs remain valid when query-expansion HITL fields are omitted."
  artifacts:
    - path: "src/backend/schemas/agent.py"
      provides: "Typed backend models for query-expansion HITL run config and resume decisions."
    - path: "src/backend/routers/agent.py"
      provides: "Router-level payload mapping for additive HITL request/resume fields."
    - path: "src/backend/agent_search/public_api.py"
      provides: "Public API handoff carrying typed query-expansion HITL data."
    - path: "sdk/core/src/schemas/agent.py"
      provides: "SDK schema parity for additive query-expansion HITL contract fields."
    - path: "src/backend/tests/api/test_agent_run.py"
      provides: "Contract compatibility assertions for run-async/run-resume payloads."
  key_links:
    - from: "src/backend/routers/agent.py"
      to: "src/backend/agent_search/public_api.py"
      via: "run-async and run-resume payload mapping"
      pattern: "sdk_run_async|sdk_resume_run"
    - from: "src/backend/schemas/agent.py"
      to: "sdk/core/src/schemas/agent.py"
      via: "field parity for query-expansion HITL models"
      pattern: "query_expansion.*hitl|checkpoint_id|approve|edit|deny|skip"
---

<objective>
Implement additive backend and SDK query-expansion HITL contract support so run-async/run-resume payloads are typed, compatible, and ready for runtime checkpoint integration.

Purpose: Establish the contract baseline required by QEH-01..QEH-05 while preserving additive and default-off compatibility.
Output: Updated backend and SDK schemas, router/public API mapping, and API contract regression coverage.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-query-expansion-hitl-end-to-end/03-RESEARCH.md
@src/backend/schemas/agent.py
@src/backend/routers/agent.py
@src/backend/agent_search/public_api.py
@src/backend/tests/api/test_agent_run.py
@sdk/core/src/schemas/agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add additive query-expansion HITL contracts across backend and SDK</name>
  <files>src/backend/schemas/agent.py, src/backend/routers/agent.py, src/backend/agent_search/public_api.py, sdk/core/src/schemas/agent.py</files>
  <action>Extend run-async request models with optional query-expansion HITL enablement fields and extend run-resume models with typed decision envelopes (`checkpoint_id`, per-expansion `approve|edit|deny|skip`, optional edited text). Keep the change additive and default-off so omitted fields preserve existing behavior. Continue to accept compatibility payloads where currently supported, and wire router-to-public-API mapping so typed data flows without ad hoc dict parsing. Mirror schema updates into SDK models in the same task to avoid backend/SDK drift.</action>
  <verify>docker compose exec backend uv run pytest src/backend/tests/api/test_agent_run.py -k "run_async or resume"</verify>
  <done>API and SDK contracts accept query-expansion HITL request/resume fields while legacy non-HITL calls still succeed unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add backend API regression coverage for additive compatibility</name>
  <files>src/backend/tests/api/test_agent_run.py</files>
  <action>Extend API tests to lock in additive compatibility for query-expansion HITL request/resume fields: new fields are accepted when present, legacy non-HITL payloads still validate and execute, and typed decision envelope shape reaches the API boundary without fallback dict parsing assumptions. Keep assertions focused on contract semantics in this plan; runtime pause/resume behavior is covered in Plan 03-02.</action>
  <verify>docker compose exec backend uv run pytest src/backend/tests/api/test_agent_run.py -k "query_expansion or run_async or resume"</verify>
  <done>Contract-focused tests prove additive compatibility and typed payload handling for query-expansion HITL API boundaries.</done>
</task>

</tasks>

<verification>
Run backend API tests for run-async and run-resume payload handling. Confirm additive query-expansion HITL fields are accepted, SDK/backend parity is preserved, and omitted HITL fields keep non-HITL behavior unchanged.
</verification>

<success_criteria>
Backend and SDK contracts are additive, typed, and compatible for query-expansion HITL configuration and resume decisions, with non-HITL defaults preserved. This plan provides the required contract baseline for runtime QEH behavior completion in Plan 03-02.
</success_criteria>

<output>
After completion, create `.planning/phases/03-query-expansion-hitl-end-to-end/03-query-expansion-hitl-end-to-end-01-SUMMARY.md`
</output>
