---
phase: 04-observability-and-remote-runtime-validation
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - scripts/validation/phase4_remote_compose.sh
  - scripts/validation/phase4_remote_sdk.sh
  - scripts/validation/phase4_collect_artifacts.py
  - .planning/phases/04-observability-and-remote-runtime-validation/04-VALIDATION-MATRIX.md
autonomous: true
must_haves:
  truths:
    - "The migrated runtime completes at least one end-to-end run successfully in remote Docker Compose."
    - "The migrated runtime completes at least one end-to-end run successfully in a fresh remote pip-installed SDK environment."
    - "Both environments produce evidence for lifecycle event visibility and trace correlation tuple integrity."
  artifacts:
    - path: "scripts/validation/phase4_remote_compose.sh"
      provides: "Automated Compose-environment execution and observability checks."
    - path: "scripts/validation/phase4_remote_sdk.sh"
      provides: "Automated fresh pip-install environment execution and observability checks."
    - path: ".planning/phases/04-observability-and-remote-runtime-validation/04-VALIDATION-MATRIX.md"
      provides: "Two-environment pass/fail matrix with evidence links and run identifiers."
  key_links:
    - from: "scripts/validation/phase4_remote_compose.sh"
      to: "scripts/validation/phase4_collect_artifacts.py"
      via: "Collect stream + trace evidence for each compose run."
      pattern: "run_id|thread_id|trace_id"
    - from: "scripts/validation/phase4_remote_sdk.sh"
      to: ".planning/phases/04-observability-and-remote-runtime-validation/04-VALIDATION-MATRIX.md"
      via: "Publish validation outcomes for pip-installed SDK environment."
      pattern: "PASS|FAIL"
---

<objective>
Prove observability and reliability behavior in required remote environments using a strict validation matrix.

Purpose: Satisfy REL-05 with deployment-realistic evidence (Compose + fresh pip SDK) beyond local-only success.
Output: Executable validation scripts and a committed matrix artifact capturing run success, lifecycle stream evidence, and trace-correlation proof for both environments.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-observability-and-remote-runtime-validation/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build remote validation scripts for Compose and fresh pip SDK environments</name>
  <files>scripts/validation/phase4_remote_compose.sh, scripts/validation/phase4_remote_sdk.sh</files>
  <action>Create executable scripts that run the same Phase 4 validation workflow in two targets: (a) remote Docker Compose deployment and (b) fresh remote virtualenv with pip-installed SDK. Each script must run health checks, execute at least one end-to-end query run, assert lifecycle stream visibility from start through terminal event, and capture run/thread/trace identifiers for evidence. Reject startup-only checks as insufficient.</action>
  <verify>bash scripts/validation/phase4_remote_compose.sh && bash scripts/validation/phase4_remote_sdk.sh</verify>
  <done>Both scripts return zero only when each environment passes full run + stream + correlation assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and persist two-environment validation matrix artifact</name>
  <files>scripts/validation/phase4_collect_artifacts.py, .planning/phases/04-observability-and-remote-runtime-validation/04-VALIDATION-MATRIX.md</files>
  <action>Implement artifact collection that records environment name, run identifiers, lifecycle stream evidence, trace-correlation evidence, and final pass/fail. Output a matrix markdown file in the Phase 4 folder with explicit criteria rows for REL-05 and links/paths to evidence files produced by Task 1 runs.</action>
  <verify>python scripts/validation/phase4_collect_artifacts.py && rg "remote-compose|pip-sdk|PASS|run_id|thread_id|trace_id" .planning/phases/04-observability-and-remote-runtime-validation/04-VALIDATION-MATRIX.md</verify>
  <done>Validation matrix exists and shows explicit PASS/FAIL status for both required environments with evidence-backed correlation and lifecycle checks.</done>
</task>

</tasks>

<verification>
Re-run both environment scripts from a clean session and confirm matrix reports PASS for each environment with concrete run identifiers and observability evidence.
</verification>

<success_criteria>
REL-05 acceptance evidence exists: same migrated runtime path is proven in remote Compose and fresh pip-installed SDK environments with end-to-end success and observability proof.
</success_criteria>

<output>
After completion, create `.planning/phases/04-observability-and-remote-runtime-validation/04-03-SUMMARY.md`
</output>
