---
phase: 04-observability-and-remote-runtime-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/backend/agent_search/runtime/graph_runner.py
  - src/backend/agent_search/runtime/lifecycle_events.py
  - src/backend/agent_search/utils/tracing.py
  - src/backend/tests/runtime/test_trace_correlation.py
  - src/backend/tests/api/test_trace_metadata_contract.py
autonomous: true
must_haves:
  truths:
    - "Operators can correlate each node execution to one run/thread/trace tuple."
    - "Terminal run outcome can be joined to node-level traces without ad-hoc lookup logic."
    - "Trace metadata contract is consistent across stream payloads, logs, and tracing backend spans."
  artifacts:
    - path: "src/backend/agent_search/utils/tracing.py"
      provides: "Trace correlation helpers and deterministic trace-id propagation."
    - path: "src/backend/agent_search/runtime/graph_runner.py"
      provides: "Node-level span metadata propagation and outcome tagging."
    - path: "src/backend/tests/runtime/test_trace_correlation.py"
      provides: "Automated proof that run/thread/trace metadata remains consistent across execution lifecycle."
  key_links:
    - from: "src/backend/agent_search/runtime/graph_runner.py"
      to: "src/backend/agent_search/utils/tracing.py"
      via: "Correlation tuple injected into every node span and terminal run observation."
      pattern: "run_id|thread_id|trace_id"
    - from: "src/backend/agent_search/runtime/lifecycle_events.py"
      to: "src/backend/agent_search/utils/tracing.py"
      via: "Event contract fields reuse tracing metadata keys for joinability."
      pattern: "trace_id|thread_id"
---

<objective>
Enforce deterministic trace correlation across runtime node execution and lifecycle outcomes.

Purpose: Satisfy OBS-02 by guaranteeing one canonical correlation tuple (`run_id`, `thread_id`, `trace_id`) across observability surfaces.
Output: Tracing propagation updates, node/outcome metadata contract enforcement, and correlation tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-observability-and-remote-runtime-validation/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize correlation tuple propagation in runtime + tracing</name>
  <files>src/backend/agent_search/runtime/graph_runner.py, src/backend/agent_search/utils/tracing.py, src/backend/agent_search/runtime/lifecycle_events.py</files>
  <action>Require and propagate `thread_id` on every graph execution path, derive/propagate deterministic `trace_id` from run root context, and ensure all node-level spans include `run_id`, `thread_id`, `trace_id`, `stage`, and `status`. Reuse the same metadata keys in lifecycle events and trace spans; do not emit per-node ad-hoc trace IDs that break run-level correlation.</action>
  <verify>docker compose exec backend uv run pytest src/backend/tests/runtime -k "trace or correlation"</verify>
  <done>Runtime and tracing code enforce one stable correlation tuple and metadata schema for node and terminal observations.</done>
</task>

<task type="auto">
  <name>Task 2: Add contract tests for correlation joinability</name>
  <files>src/backend/tests/runtime/test_trace_correlation.py, src/backend/tests/api/test_trace_metadata_contract.py</files>
  <action>Add tests proving that a single run produces node-level and terminal records that all carry the same correlation tuple and can be joined deterministically. Include failure-path assertions so error outcomes still preserve run/thread/trace correlation fields.</action>
  <verify>docker compose exec backend uv run pytest src/backend/tests/runtime/test_trace_correlation.py src/backend/tests/api/test_trace_metadata_contract.py</verify>
  <done>Automated tests fail if any execution path drops/mutates `run_id`, `thread_id`, or `trace_id` metadata.</done>
</task>

</tasks>

<verification>
Execute one successful and one failed run, then confirm both stream events and trace records share the same correlation tuple and include terminal outcome metadata.
</verification>

<success_criteria>
OBS-02 acceptance evidence exists: node execution traces, thread context, and final run outcomes are correlated through a stable metadata contract.
</success_criteria>

<output>
After completion, create `.planning/phases/04-observability-and-remote-runtime-validation/04-02-SUMMARY.md`
</output>
