---
phase: 04-observability-and-remote-runtime-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/agent_search/runtime/lifecycle_events.py
  - src/backend/agent_search/runtime/graph_runner.py
  - src/backend/agent_search/routers/runs.py
  - src/backend/tests/api/test_run_events_stream.py
autonomous: true
must_haves:
  truths:
    - "Operators can observe ordered lifecycle progress for a run from start through completion/failure."
    - "Every streamed lifecycle event includes run/thread/trace correlation fields."
    - "Event streams survive reconnect without duplicating or skipping lifecycle steps."
  artifacts:
    - path: "src/backend/agent_search/runtime/lifecycle_events.py"
      provides: "Canonical lifecycle event envelope and sequence/id handling."
    - path: "src/backend/agent_search/routers/runs.py"
      provides: "SSE endpoint that emits run lifecycle events."
    - path: "src/backend/tests/api/test_run_events_stream.py"
      provides: "Contract and ordering tests for lifecycle stream behavior."
  key_links:
    - from: "src/backend/agent_search/runtime/graph_runner.py"
      to: "src/backend/agent_search/runtime/lifecycle_events.py"
      via: "Graph stage transitions mapped to lifecycle event envelope."
      pattern: "emit_lifecycle_event|build_lifecycle_event"
    - from: "src/backend/agent_search/routers/runs.py"
      to: "src/backend/agent_search/runtime/lifecycle_events.py"
      via: "SSE route subscribes to run event stream and yields event ids."
      pattern: "text/event-stream|Last-Event-ID"
---

<objective>
Establish a deterministic runtime lifecycle streaming contract for LangGraph execution progress.

Purpose: Satisfy OBS-01 with real event streaming semantics rather than stage polling snapshots.
Output: Canonical lifecycle event envelope, SSE endpoint wiring, and contract tests for ordering/resume.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-observability-and-remote-runtime-validation/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement canonical lifecycle event contract</name>
  <files>src/backend/agent_search/runtime/lifecycle_events.py, src/backend/agent_search/runtime/graph_runner.py</files>
  <action>Create a run-scoped lifecycle event envelope that includes `event_type`, `event_id`, `run_id`, `thread_id`, `trace_id`, `stage`, `status`, `emitted_at`, and optional `error`. Ensure event IDs are monotonic per run and emitted on start, each stage transition, retry/recovery transitions, and terminal completion/failure. Use LangGraph-native stream signals (`updates`, `tasks`, `checkpoints`) as source semantics; do not implement synthetic polling-only events.</action>
  <verify>docker compose exec backend uv run pytest src/backend/tests/runtime -k "lifecycle or event"</verify>
  <done>Runtime contains one canonical lifecycle event builder and emits ordered, deterministic lifecycle events tied to run/thread/trace identity.</done>
</task>

<task type="auto">
  <name>Task 2: Expose lifecycle stream endpoint with reconnect safety</name>
  <files>src/backend/agent_search/routers/runs.py, src/backend/tests/api/test_run_events_stream.py</files>
  <action>Add/extend SSE endpoint for run events (`text/event-stream`) that emits event IDs and honors `Last-Event-ID` resume behavior to avoid duplicate/skipped lifecycle records after reconnect. Ensure stream payload mirrors the canonical event contract from Task 1 and includes terminal event emission on completion/failure.</action>
  <verify>docker compose exec backend uv run pytest src/backend/tests/api/test_run_events_stream.py</verify>
  <done>Operators can subscribe to run lifecycle stream and observe an ordered start->...->terminal timeline with reconnect-safe continuity.</done>
</task>

</tasks>

<verification>
Run one end-to-end query and confirm stream emits start, intermediate stage, and terminal events in order, each with `run_id`, `thread_id`, and `trace_id`.
</verification>

<success_criteria>
OBS-01 acceptance evidence exists: lifecycle stream is event-driven, ordered, and reconnect-safe; event payload contract is stable and correlation-ready.
</success_criteria>

<output>
After completion, create `.planning/phases/04-observability-and-remote-runtime-validation/04-01-SUMMARY.md`
</output>
