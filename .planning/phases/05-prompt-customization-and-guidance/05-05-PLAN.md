---
phase: 05-prompt-customization-and-guidance
plan: 05
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/backend/agent_search/public_api.py
  - sdk/core/src/agent_search/public_api.py
  - src/backend/tests/sdk/test_public_api.py
  - src/backend/tests/sdk/test_public_api_async.py
autonomous: true
must_haves:
  truths:
    - "SDK callers can keep mutable prompt defaults in a custom-prompts map and have them applied on each run."
    - "Per-run prompt overrides deterministically win over SDK/client defaults while preserving existing thread_id behavior."
    - "Sync and async public API paths resolve prompt precedence identically."
  artifacts:
    - path: "src/backend/agent_search/public_api.py"
      provides: "Backend SDK entrypoint merge logic for defaults and per-run prompt overrides."
    - path: "sdk/core/src/agent_search/public_api.py"
      provides: "Core SDK merge logic for mutable prompt defaults and per-call overrides."
    - path: "src/backend/tests/sdk/test_public_api.py"
      provides: "Sync regression tests for precedence and mutable-default isolation."
    - path: "src/backend/tests/sdk/test_public_api_async.py"
      provides: "Async regression tests for precedence and mutable-default isolation."
  key_links:
    - from: "src/backend/agent_search/public_api.py"
      to: "src/backend/agent_search/config.py"
      via: "entrypoint uses typed RuntimeConfig prompt map while merging caller defaults and per-run overrides"
      pattern: "RuntimeConfig|custom_prompts|merge"
    - from: "sdk/core/src/agent_search/public_api.py"
      to: "sdk/core/src/agent_search/config.py"
      via: "SDK entrypoint resolves mutable default map and per-call overrides deterministically"
      pattern: "RuntimeConfig\\.from_dict|custom_prompts|copy"
---

<objective>
Implement SDK/public API prompt precedence merge plumbing as a focused follow-on to contract foundation.

Purpose: PRM-03 requires deterministic precedence and mutable-default isolation behavior, which is best validated independently from request contract parsing.
Output: Public API merge logic in backend + core SDK and focused sync/async precedence regression tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-prompt-customization-and-guidance/05-RESEARCH.md
@.planning/phases/05-prompt-customization-and-guidance/05-01-SUMMARY.md
@src/backend/agent_search/public_api.py
@sdk/core/src/agent_search/public_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement deterministic prompt precedence merge in sync and async public APIs</name>
  <files>src/backend/agent_search/public_api.py, sdk/core/src/agent_search/public_api.py</files>
  <action>Update backend and core SDK public API entrypoints so effective prompt resolution follows built-in defaults -> SDK/client `custom_prompts` defaults map -> per-run overrides. Preserve existing `thread_id` behavior and do not introduce new client classes in this phase. Defensively copy caller-owned mutable maps at merge boundaries to avoid cross-run mutation bleed.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/sdk/test_public_api.py src/backend/tests/sdk/test_public_api_async.py -k "config or precedence or prompt"`.</verify>
  <done>Sync and async entrypoints resolve prompt precedence identically and isolate mutable default maps across runs.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused precedence and mutable-default isolation regressions</name>
  <files>src/backend/tests/sdk/test_public_api.py, src/backend/tests/sdk/test_public_api_async.py</files>
  <action>Add tests that verify per-run values override SDK defaults, default maps persist when no per-run override is supplied, and one run cannot mutate another run's default prompt map. Keep tests at merge-plumbing level only; runtime prompt influence and citation guardrail behavior are validated in runtime wiring plans.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/sdk/test_public_api.py src/backend/tests/sdk/test_public_api_async.py`.</verify>
  <done>Regression suite proves precedence semantics and mutable-map isolation required by PRM-03.</done>
</task>

</tasks>

<verification>
Public API merge plumbing is deterministic and parity-safe across sync and async paths.
</verification>

<success_criteria>
PRM-03 merge behavior is complete: SDK/client prompt defaults and per-run overrides are resolved predictably without mutable-state leakage.
</success_criteria>

<output>
After completion, create `.planning/phases/05-prompt-customization-and-guidance/05-05-SUMMARY.md`
</output>
