---
phase: 05-prompt-customization-and-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/schemas/agent.py
  - src/backend/routers/agent.py
  - src/backend/agent_search/config.py
  - sdk/core/src/agent_search/config.py
  - src/backend/tests/api/test_agent_run.py
  - src/backend/tests/sdk/test_runtime_config.py
autonomous: true
must_haves:
  truths:
    - "Run payloads can carry custom prompt overrides for subanswer and synthesis without breaking legacy payloads."
    - "API input accepts both `custom_prompts` and `custom-prompts` while normalizing to one canonical internal shape."
    - "Runtime config parsing in backend and SDK recognizes supported prompt keys and safely ignores unknown keys."
  artifacts:
    - path: "src/backend/schemas/agent.py"
      provides: "Additive request contract for prompt overrides with alias support."
    - path: "src/backend/agent_search/config.py"
      provides: "Typed RuntimeConfig parsing for custom prompt keys and safe defaults."
    - path: "sdk/core/src/agent_search/config.py"
      provides: "SDK RuntimeConfig parity for custom prompt parsing and default handling."
    - path: "src/backend/routers/agent.py"
      provides: "Router config builder that forwards prompt overrides into SDK config."
  key_links:
    - from: "src/backend/schemas/agent.py"
      to: "src/backend/routers/agent.py"
      via: "RuntimeAgentRunRequest prompt map is accepted and forwarded in config payload"
      pattern: "custom_prompts|custom-prompts"
    - from: "src/backend/routers/agent.py"
      to: "src/backend/agent_search/config.py"
      via: "router-provided payload is parsed through RuntimeConfig before runtime execution"
      pattern: "RuntimeConfig\\.from_dict|custom_prompts"
---

<objective>
Create the additive prompt-customization contract and typed config foundation for API and SDK surfaces.

Purpose: PRM-03 requires explicit, backward-compatible prompt inputs before any precedence merge or runtime wiring can safely build on top.
Output: `custom_prompts`/`custom-prompts` request compatibility, typed parsing parity in backend + SDK config, and contract-level regression tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-prompt-customization-and-guidance/05-RESEARCH.md
@src/backend/schemas/agent.py
@src/backend/routers/agent.py
@src/backend/agent_search/config.py
@sdk/core/src/agent_search/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed custom prompt request contract with canonical keys and aliases</name>
  <files>src/backend/schemas/agent.py, src/backend/agent_search/config.py, sdk/core/src/agent_search/config.py</files>
  <action>Add a typed two-key prompt contract (`subanswer`, `synthesis`) and thread it into runtime config parsing. Keep canonical internal naming as `custom_prompts` while accepting `custom-prompts` as an input alias on request-facing models. Preserve additive behavior: payloads with only `query` and optional `thread_id` must still validate unchanged. Enforce map hygiene by ignoring unknown prompt keys instead of failing the whole request.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/sdk/test_runtime_config.py src/backend/tests/api/test_agent_run.py -k "runtime_config or run"` and confirm defaults still pass while prompt fields are accepted.</verify>
  <done>Runtime config models in backend and SDK both parse optional `custom_prompts` with `subanswer` and `synthesis` keys, and legacy payloads remain valid.</done>
</task>

<task type="auto">
  <name>Task 2: Keep router forwarding additive and thread-id-safe</name>
  <files>src/backend/routers/agent.py, src/backend/tests/api/test_agent_run.py</files>
  <action>Update router config assembly so accepted prompt maps are forwarded into runtime config payloads for both sync and async run entrypoints while preserving existing `thread_id` behavior and idempotency expectations. Keep this task limited to request-to-config forwarding only; do not add precedence merge logic in public API files here.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/api/test_agent_run.py -k "run and custom"`.</verify>
  <done>Run endpoints accept prompt map inputs and pass normalized values into runtime config without regressions to existing run request handling.</done>
</task>

<task type="auto">
  <name>Task 3: Add contract regression tests for aliasing and additive compatibility</name>
  <files>src/backend/tests/api/test_agent_run.py, src/backend/tests/sdk/test_runtime_config.py</files>
  <action>Add focused tests that cover accepting both `custom-prompts` and `custom_prompts`, ensuring unknown keys are ignored safely, and confirming legacy payload compatibility when prompt maps are omitted. Keep precedence and mutable-default isolation checks out of this task; those belong to public API merge plumbing.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/api/test_agent_run.py src/backend/tests/sdk/test_runtime_config.py`.</verify>
  <done>Contract tests prove additive compatibility and canonical/alias handling required before merge-precedence behavior is introduced.</done>
</task>

</tasks>

<verification>
API and SDK config layers accept prompt overrides additively with alias compatibility and preserve existing run behavior when no prompt map is provided.
</verification>

<success_criteria>
PRM-03 contract foundation is complete: request/config surfaces are typed and backward compatible, enabling dedicated merge-precedence implementation in a follow-on plan.
</success_criteria>

<output>
After completion, create `.planning/phases/05-prompt-customization-and-guidance/05-01-SUMMARY.md`
</output>
