---
phase: 05-prompt-customization-and-guidance
plan: 03
type: execute
wave: 4
depends_on: ["05-04", "05-05"]
files_modified:
  - README.md
  - sdk/core/README.md
  - sdk/python/README.md
  - docs/prompt-customization.md
autonomous: true
must_haves:
  truths:
    - "Consumers can see the default subanswer and synthesis prompt responsibilities before overriding them."
    - "Consumers can see and apply precedence rules (built-in defaults -> SDK/client defaults -> per-run overrides)."
    - "Consumers are explicitly warned that prompt overrides do not bypass citation/fallback code-level safeguards."
  artifacts:
    - path: "docs/prompt-customization.md"
      provides: "Canonical prompt customization guide with defaults, scope boundaries, and override precedence."
    - path: "sdk/core/README.md"
      provides: "SDK usage examples for mutable custom-prompts defaults and per-call override patterns."
    - path: "sdk/python/README.md"
      provides: "Python client guidance mirroring SDK prompt customization contract."
    - path: "README.md"
      provides: "Top-level pointer to prompt customization docs and safety caveats."
  key_links:
    - from: "docs/prompt-customization.md"
      to: "sdk/core/README.md"
      via: "shared examples use same custom prompt keys and precedence order"
      pattern: "custom-prompts|custom_prompts|subanswer|synthesis"
    - from: "docs/prompt-customization.md"
      to: "src/backend/agent_search/runtime/nodes/answer.py"
      via: "documentation states citation guardrails remain code-level"
      pattern: "citation|fallback|guardrail"
---

<objective>
Publish prompt customization guidance so API/SDK consumers can safely use overrides with clear defaults and responsibilities.

Purpose: PRM-04 requires operational clarity for consumers, not only implementation correctness.
Output: One canonical prompt customization doc plus SDK and root README updates with aligned examples and safety notes.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-prompt-customization-and-guidance/05-RESEARCH.md
@.planning/phases/05-prompt-customization-and-guidance/05-04-SUMMARY.md
@.planning/phases/05-prompt-customization-and-guidance/05-05-SUMMARY.md
@README.md
@sdk/core/README.md
@sdk/python/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical prompt customization guide</name>
  <files>docs/prompt-customization.md</files>
  <action>Create a dedicated guide documenting: (1) the two supported prompt keys (`subanswer`, `synthesis`), (2) each prompt's responsibility boundary, (3) default behavior when unset, and (4) precedence resolution order. Include both accepted input forms (`custom-prompts` and `custom_prompts`) and name one canonical external form for examples. Keep this guide scoped to prompt customization only; do not add HITL control loop material.</action>
  <verify>Run `rg -n "subanswer|synthesis|precedence|custom-prompts|custom_prompts" docs/prompt-customization.md` and confirm all required sections are present.</verify>
  <done>Guide exists with explicit defaults, scope boundaries, key naming guidance, and precedence rules aligned to implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Update SDK docs with mutable defaults and per-run override examples</name>
  <files>sdk/core/README.md, sdk/python/README.md</files>
  <action>Add executable-style examples showing SDK consumers how to keep a mutable `custom_prompts` map as client-level defaults and optionally override values per run. Explicitly call out merge behavior and map-isolation expectations. Do not introduce a new SDK class in docs for this phase; document the existing config-map workflow to avoid contract drift.</action>
  <verify>Run `rg -n "custom_prompts|custom-prompts|subanswer|synthesis|override" sdk/core/README.md sdk/python/README.md`.</verify>
  <done>SDK READMEs clearly show PRM-03 usage patterns and align with the implemented API surface.</done>
</task>

<task type="auto">
  <name>Task 3: Add top-level documentation pointer and safety responsibilities</name>
  <files>README.md</files>
  <action>Add a concise section/link directing consumers to the prompt customization guide and summarize safety responsibilities: prompts influence generation strategy, while citation/fallback enforcement remains runtime code-level and should not be bypassed by prompt edits.</action>
  <verify>Run `rg -n "prompt customization|custom prompts|citation|fallback" README.md`.</verify>
  <done>Top-level docs route consumers to canonical guidance and explicitly set safe-use expectations.</done>
</task>

</tasks>

<verification>
Documentation consistently explains prompt defaults, scope, precedence, and safety boundaries across root and SDK surfaces.
</verification>

<success_criteria>
PRM-04 is complete: consumers can understand and correctly apply prompt overrides with clear defaults and explicit safety constraints.
</success_criteria>

<output>
After completion, create `.planning/phases/05-prompt-customization-and-guidance/05-03-SUMMARY.md`
</output>
