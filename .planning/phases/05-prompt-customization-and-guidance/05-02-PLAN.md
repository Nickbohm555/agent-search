---
phase: 05-prompt-customization-and-guidance
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/backend/services/subanswer_service.py
  - src/backend/services/initial_answer_service.py
  - src/backend/tests/services/test_subanswer_service.py
  - src/backend/tests/services/test_initial_answer_service.py
autonomous: true
must_haves:
  truths:
    - "Subanswer generation accepts an explicit effective prompt template without changing default behavior when unset."
    - "Synthesis generation accepts an explicit effective prompt template without changing default behavior when unset."
    - "No-key and no-evidence fallback behavior remains unchanged by service signature updates."
  artifacts:
    - path: "src/backend/services/subanswer_service.py"
      provides: "Subanswer generation accepts effective prompt template while retaining fallback behavior."
    - path: "src/backend/services/initial_answer_service.py"
      provides: "Final synthesis generation accepts effective prompt template while retaining fallback behavior."
    - path: "src/backend/tests/services/test_subanswer_service.py"
      provides: "Service-level regression tests for prompt-parameter acceptance and fallback safety."
    - path: "src/backend/tests/services/test_initial_answer_service.py"
      provides: "Service-level regression tests for synthesis prompt-parameter acceptance and default parity."
  key_links:
    - from: "src/backend/services/subanswer_service.py"
      to: "src/backend/tests/services/test_subanswer_service.py"
      via: "tests verify optional prompt-template parameter preserves fallback/default paths"
      pattern: "prompt|fallback|no key|no evidence"
    - from: "src/backend/services/initial_answer_service.py"
      to: "src/backend/tests/services/test_initial_answer_service.py"
      via: "tests verify synthesis prompt parameter uses default template when override is absent"
      pattern: "prompt|default|synthesis"
---

<objective>
Introduce explicit prompt-template parameters at generation service boundaries with safe defaults.

Purpose: PRM-01 and PRM-02 require prompt influence to flow through services, but service signatures should be stabilized before node wiring and integration behavior tests.
Output: Prompt-aware service interfaces and service-level regressions that preserve existing default and fallback semantics.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-prompt-customization-and-guidance/05-RESEARCH.md
@.planning/phases/05-prompt-customization-and-guidance/05-01-SUMMARY.md
@src/backend/services/subanswer_service.py
@src/backend/services/initial_answer_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add prompt-template parameters to generation services with safe defaults</name>
  <files>src/backend/services/subanswer_service.py, src/backend/services/initial_answer_service.py, src/backend/tests/services/test_subanswer_service.py, src/backend/tests/services/test_initial_answer_service.py</files>
  <action>Refactor generation services to expose explicit optional prompt-template inputs for subanswer and synthesis generation, defaulting to current built-in templates when no override is supplied. Keep fallback paths unchanged for no-doc and no-API-key scenarios. Do not move citation/fallback contract logic from runtime nodes into prompt text.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/services/test_subanswer_service.py src/backend/tests/services/test_initial_answer_service.py`.</verify>
  <done>Services accept prompt overrides without changing default behavior, and existing fallback behavior remains intact under no-key/no-evidence conditions.</done>
</task>

<task type="auto">
  <name>Task 2: Harden service-level regressions for default parity and fallback safety</name>
  <files>src/backend/tests/services/test_subanswer_service.py, src/backend/tests/services/test_initial_answer_service.py</files>
  <action>Add targeted assertions that compare outputs between unset prompt overrides and existing default templates, and verify fallback branches remain stable when prompt parameters are provided but evidence/API-key conditions fail. Keep these tests at service boundary level; runtime node wiring and end-to-end influence verification belong to follow-on plans.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/services/test_subanswer_service.py src/backend/tests/services/test_initial_answer_service.py -k "prompt or fallback or default"`.</verify>
  <done>Service tests lock in prompt-parameter behavior and prove no regression to baseline generation safety semantics.</done>
</task>

</tasks>

<verification>
Generation services expose prompt-override parameters safely, with default parity and fallback behavior protected by service-level regression tests.
</verification>

<success_criteria>
Service signature groundwork for PRM-01/02 is complete and ready for dedicated runtime wiring and integration-level behavior verification.
</success_criteria>

<output>
After completion, create `.planning/phases/05-prompt-customization-and-guidance/05-02-SUMMARY.md`
</output>
