---
phase: 02-subquestion-hitl-end-to-end
plan: 02
type: execute
wave: 3
depends_on:
  - "02-04"
files_modified:
  - src/frontend/src/utils/api.ts
  - src/frontend/src/App.tsx
  - src/frontend/src/App.test.tsx
autonomous: true
must_haves:
  truths:
    - "When a run pauses for subquestion HITL, the frontend presents actionable review controls rather than a failure state."
    - "Users can submit approve/edit/deny/skip decisions and resume execution from the same paused run."
    - "Non-HITL runs still follow existing UI flow with no new required interaction."
  artifacts:
    - path: "src/frontend/src/utils/api.ts"
      provides: "Typed client contracts and request builders for HITL enablement and resume decisions."
    - path: "src/frontend/src/App.tsx"
      provides: "Paused subquestion review panel with approve/edit/deny actions and resume trigger."
    - path: "src/frontend/src/App.test.tsx"
      provides: "UI regression tests for paused-review and resume transitions."
  key_links:
    - from: "src/frontend/src/App.tsx"
      to: "src/frontend/src/utils/api.ts"
      via: "resume submission handler"
      pattern: "startAgentRun|subscribeToAgentRunEvents|run-resume"
    - from: "src/frontend/src/utils/api.ts"
      to: "/api/agents/run-events/{job_id}"
      via: "typed paused payload parsing"
      pattern: "run.paused|interrupt_payload|checkpoint_id"
    - from: "src/frontend/src/App.test.tsx"
      to: "src/frontend/src/App.tsx"
      via: "paused review interaction assertions"
      pattern: "approve|edit|deny|skip"
---

<objective>
Implement the frontend subquestion HITL review-and-resume workflow so paused runs become actionable and can continue with explicit user decisions.

Purpose: Deliver the user-facing half of SQH-02..SQH-05 using the backend contracts and runtime checkpoint payload from Plans 02-01 and 02-04.
Output: Updated frontend API typings, review UI state transitions, and integration-style component tests for resume behavior.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-subquestion-hitl-end-to-end/02-RESEARCH.md
@.planning/phases/02-subquestion-hitl-end-to-end/02-04-PLAN.md
@src/frontend/src/utils/api.ts
@src/frontend/src/App.tsx
@src/frontend/src/App.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed frontend HITL request and paused payload contracts</name>
  <files>src/frontend/src/utils/api.ts</files>
  <action>Extend frontend API types and request builders to include optional subquestion HITL enablement on run start and typed decision envelope on run resume. Update runtime event/status guards so paused payload fields (`checkpoint_id`, proposed subquestions with stable IDs or index mapping) are validated and passed through as first-class data instead of opaque unknown blobs.</action>
  <verify>docker compose exec frontend npm run typecheck</verify>
  <done>Frontend API layer can safely start HITL-enabled runs, parse paused payloads, and submit typed resume decisions without `any`-style handling.</done>
</task>

<task type="auto">
  <name>Task 2: Implement paused review UX with approve/edit/deny/skip actions</name>
  <files>src/frontend/src/App.tsx</files>
  <action>Add a paused-review UI state that is entered on `run.paused` with subquestion payload and treated as actionable (not terminal failure). Render per-item controls for approve, edit text, and deny; include skip-to-default continuation behavior when requested. Submit resume payloads bound to `job_id` + `checkpoint_id`, then transition back to active stream handling until completion.</action>
  <verify>docker compose exec frontend npm run test -- App.test.tsx</verify>
  <done>Users can review subquestions during pause, submit decisions, and observe the run continue from paused -> running -> completed.</done>
</task>

<task type="auto">
  <name>Task 3: Add frontend regression tests for paused and non-HITL flows</name>
  <files>src/frontend/src/App.test.tsx</files>
  <action>Expand integration-style tests to cover: paused payload renders review panel, approve/edit/deny/skip interactions create expected resume payload, resumed stream progresses to completion, and non-HITL runs still complete without showing review UI. Ensure tests assert that paused state does not surface as generic error to users.</action>
  <verify>docker compose exec frontend npm run test -- App.test.tsx && docker compose exec frontend npm run typecheck</verify>
  <done>Frontend tests protect both HITL and default flows, including actionable pause semantics and correct resume payload shape.</done>
</task>

</tasks>

<verification>
Run frontend test and typecheck commands. Confirm pause renders review UI, decision submission resumes run, and standard non-HITL runs behave exactly as before.
</verification>

<success_criteria>
SQH user workflows are usable in the app: pause is actionable, approve/edit/deny/skip are all represented in UI state and API payloads, and existing non-HITL behavior remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/02-subquestion-hitl-end-to-end/02-subquestion-hitl-end-to-end-02-SUMMARY.md`
</output>
