---
phase: 02-subquestion-hitl-end-to-end
plan: 04
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/backend/agent_search/runtime/jobs.py
  - src/backend/agent_search/runtime/resume.py
  - src/backend/agent_search/runtime/graph/routes.py
  - src/backend/agent_search/runtime/graph/builder.py
  - src/backend/agent_search/runtime/graph/execution.py
  - src/backend/agent_search/runtime/lifecycle_events.py
  - src/backend/tests/api/test_run_events_stream.py
autonomous: true
must_haves:
  truths:
    - "When subquestion HITL is enabled for a run, execution pauses after decompose and before downstream fan-out."
    - "Approve/edit/deny/skip decisions submitted to run-resume deterministically control which subquestions continue."
    - "Paused lifecycle/event payloads include checkpoint identity and subquestion data required for downstream clients."
  artifacts:
    - path: "src/backend/agent_search/runtime/graph/routes.py"
      provides: "Single checkpoint gate inserted after decompose and before lane fan-out."
    - path: "src/backend/agent_search/runtime/jobs.py"
      provides: "Initial-run checkpoint path selection and resume application lifecycle."
    - path: "src/backend/agent_search/runtime/resume.py"
      provides: "Deterministic decision application for approve/edit/deny/skip."
    - path: "src/backend/tests/api/test_run_events_stream.py"
      provides: "SSE/lifecycle regression coverage for pause/resume checkpoint semantics."
  key_links:
    - from: "src/backend/agent_search/runtime/graph/routes.py"
      to: "src/backend/agent_search/runtime/jobs.py"
      via: "checkpoint payload propagation and pause transition"
      pattern: "subquestions_ready|interrupt_payload|checkpoint_id"
    - from: "src/backend/agent_search/runtime/jobs.py"
      to: "src/backend/agent_search/runtime/resume.py"
      via: "resume decision application into downstream execution inputs"
      pattern: "approve|edit|deny|skip"
    - from: "src/backend/agent_search/runtime/lifecycle_events.py"
      to: "src/backend/tests/api/test_run_events_stream.py"
      via: "paused event payload contract assertions"
      pattern: "run.paused|interrupt_payload|checkpoint_id"
---

<objective>
Implement runtime checkpoint wiring for subquestion HITL so enabled runs pause on initial execution and resume deterministically from typed decisions.

Purpose: Deliver SQH-02..SQH-05 runtime behavior on top of Plan 02-01 contract foundations.
Output: Updated runtime checkpoint routing, pause/resume lifecycle propagation, and SSE regression coverage for checkpoint payload semantics.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-subquestion-hitl-end-to-end/02-RESEARCH.md
@.planning/phases/02-subquestion-hitl-end-to-end/02-01-PLAN.md
@src/backend/agent_search/runtime/jobs.py
@src/backend/agent_search/runtime/resume.py
@src/backend/agent_search/runtime/graph/routes.py
@src/backend/tests/api/test_run_events_stream.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert the decompose-to-fanout subquestion checkpoint gate</name>
  <files>src/backend/agent_search/runtime/graph/routes.py, src/backend/agent_search/runtime/graph/builder.py, src/backend/agent_search/runtime/graph/execution.py</files>
  <action>Wire a single checkpoint node/route immediately after decompose and before lane fan-out. Ensure HITL-enabled initial runs enter the checkpoint-capable execution path, while non-HITL runs continue through existing non-checkpoint routing unchanged.</action>
  <verify>docker compose exec backend uv run pytest src/backend/tests/api/test_run_events_stream.py -k "checkpoint or paused"</verify>
  <done>Runtime graph pauses exactly once at `subquestions_ready` for HITL-enabled runs and preserves legacy flow when HITL is disabled.</done>
</task>

<task type="auto">
  <name>Task 2: Apply typed resume decisions and emit paused lifecycle metadata</name>
  <files>src/backend/agent_search/runtime/jobs.py, src/backend/agent_search/runtime/resume.py, src/backend/agent_search/runtime/lifecycle_events.py</files>
  <action>Route initial-run pause and explicit resume through a shared typed decision path. Apply approve/edit/deny/skip deterministically, persist/emit `interrupt_payload` and `checkpoint_id`, and ensure resumed execution consumes the filtered/edited subquestion set without ad hoc transforms.</action>
  <verify>docker compose exec backend uv run pytest src/backend/tests/api/test_run_events_stream.py -k "resume or interrupt_payload or checkpoint_id"</verify>
  <done>Resume transitions produce deterministic downstream subquestions and paused payloads always include checkpoint metadata needed by clients.</done>
</task>

<task type="auto">
  <name>Task 3: Add SSE/lifecycle regressions for paused and resumed runtime behavior</name>
  <files>src/backend/tests/api/test_run_events_stream.py</files>
  <action>Expand event-stream tests to assert paused event shape (`checkpoint_id`, proposed subquestions), decision-driven resume behavior for approve/edit/deny/skip, and non-HITL pass-through behavior with no pause event.</action>
  <verify>docker compose exec backend uv run pytest src/backend/tests/api/test_run_events_stream.py</verify>
  <done>API/SSE regressions protect runtime pause/resume semantics and checkpoint payload structure required by frontend and SDK consumers.</done>
</task>

</tasks>

<verification>
Run backend event-stream regressions to confirm enabled runs pause after decompose, resume decisions alter downstream execution deterministically, and non-HITL runs remain unchanged.
</verification>

<success_criteria>
SQH-02..SQH-05 runtime requirements are satisfied: initial HITL pause occurs in the right graph position, typed decisions control continuation, checkpoint metadata is emitted for clients, and default non-HITL behavior remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/02-subquestion-hitl-end-to-end/02-subquestion-hitl-end-to-end-04-SUMMARY.md`
</output>
