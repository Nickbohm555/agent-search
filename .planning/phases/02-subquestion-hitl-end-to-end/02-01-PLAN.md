---
phase: 02-subquestion-hitl-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/schemas/agent.py
  - src/backend/routers/agent.py
  - src/backend/agent_search/public_api.py
  - src/backend/tests/api/test_agent_run.py
autonomous: true
must_haves:
  truths:
    - "Backend run-async accepts optional per-run subquestion HITL controls without changing default behavior."
    - "Backend run-resume accepts typed approve/edit/deny/skip decision envelopes with stable validation errors on malformed payloads."
    - "Default runs that omit HITL controls continue through non-HITL behavior unchanged."
  artifacts:
    - path: "src/backend/schemas/agent.py"
      provides: "Typed request and resume models for subquestion HITL configuration and decisions."
    - path: "src/backend/agent_search/public_api.py"
      provides: "Public SDK-facing argument normalization and pass-through for typed HITL payloads."
    - path: "src/backend/tests/api/test_agent_run.py"
      provides: "API-level contract regression coverage for additive run/resume payloads."
  key_links:
    - from: "src/backend/routers/agent.py"
      to: "src/backend/agent_search/public_api.py"
      via: "run-async and run-resume request mapping"
      pattern: "sdk_run_async|sdk_resume_run"
    - from: "src/backend/routers/agent.py"
      to: "src/backend/schemas/agent.py"
      via: "typed decision envelope validation"
      pattern: "approve|edit|deny|skip"
---

<objective>
Implement additive backend contract support for subquestion HITL run and resume payloads without changing runtime checkpoint execution semantics.

Purpose: Establish the API contract foundation required by SQH-01 and resume decision typing before runtime wiring is introduced.
Output: Updated backend schemas/router/public API mapping plus API contract regression coverage for additive/default-off behavior.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-subquestion-hitl-end-to-end/02-RESEARCH.md
@src/backend/schemas/agent.py
@src/backend/routers/agent.py
@src/backend/agent_search/public_api.py
@src/backend/tests/api/test_agent_run.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add additive subquestion HITL API contracts</name>
  <files>src/backend/schemas/agent.py, src/backend/routers/agent.py, src/backend/agent_search/public_api.py</files>
  <action>Extend run-async request contracts with optional per-run subquestion HITL controls and extend run-resume contracts with a typed decision envelope (`checkpoint_id`, per-subquestion `approve|edit|deny|skip`, optional edited text). Keep the change additive and default-off: omitted fields must preserve current behavior. Preserve compatibility by continuing to accept legacy `resume=True` payloads where required. Wire router -> SDK public API mapping so typed values are passed through without ad hoc dict parsing.</action>
  <verify>docker compose exec backend uv run pytest src/backend/tests/api/test_agent_run.py -k "run_async or resume"</verify>
  <done>Run request and resume payloads accept typed HITL data; old payloads still succeed without enabling HITL.</done>
</task>

<task type="auto">
  <name>Task 2: Add contract-focused API regressions for additive and typed behavior</name>
  <files>src/backend/tests/api/test_agent_run.py</files>
  <action>Add or update API tests to cover: run-async payload accepts optional HITL config, run-resume payload accepts typed decisions (`approve|edit|deny|skip`), malformed envelopes return explicit validation errors, and legacy/default payload shapes remain accepted. Keep these tests scoped to request/response contract behavior (not runtime pause sequencing, which is covered in Plan 02-04).</action>
  <verify>docker compose exec backend uv run pytest src/backend/tests/api/test_agent_run.py -k "hitl or resume or validation"</verify>
  <done>Backend API tests enforce additive contract compatibility and typed decision validation at request boundaries.</done>
</task>

</tasks>

<verification>
Run backend API contract tests for run-async/run-resume payloads and validation behavior. Confirm omitted HITL fields preserve existing behavior and typed envelopes are accepted/rejected deterministically.
</verification>

<success_criteria>
SQH contract prerequisites are satisfied on backend surfaces: per-run enablement fields and typed resume decisions exist, malformed payloads fail predictably, and default behavior remains unchanged when HITL is not enabled.
</success_criteria>

<output>
After completion, create `.planning/phases/02-subquestion-hitl-end-to-end/02-subquestion-hitl-end-to-end-01-SUMMARY.md`
</output>
