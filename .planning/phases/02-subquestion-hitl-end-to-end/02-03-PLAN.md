---
phase: 02-subquestion-hitl-end-to-end
plan: 03
type: execute
wave: 3
depends_on:
  - "02-04"
files_modified:
  - sdk/core/src/schemas/agent.py
  - sdk/core/src/agent_search/public_api.py
  - sdk/core/src/agent_search/runtime/jobs.py
  - src/backend/tests/sdk/test_public_api_async.py
  - src/backend/tests/sdk/test_sdk_async_e2e.py
autonomous: true
must_haves:
  truths:
    - "SDK consumers can enable subquestion HITL per run using typed request parameters."
    - "SDK consumers can resume paused runs using typed approve/edit/deny/skip decisions."
    - "SDK async behavior remains backward-compatible when HITL options are omitted."
  artifacts:
    - path: "sdk/core/src/schemas/agent.py"
      provides: "SDK schema parity for subquestion HITL run controls and resume decision envelope."
    - path: "sdk/core/src/agent_search/public_api.py"
      provides: "SDK public async API support for HITL start and resume semantics."
    - path: "src/backend/tests/sdk/test_public_api_async.py"
      provides: "Contract regression tests for SDK async HITL flows."
  key_links:
    - from: "sdk/core/src/agent_search/public_api.py"
      to: "sdk/core/src/schemas/agent.py"
      via: "typed request/resume models"
      pattern: "run_async|resume_run|RuntimeAgentRunResumeRequest"
    - from: "sdk/core/src/agent_search/runtime/jobs.py"
      to: "src/backend/agent_search/runtime/jobs.py"
      via: "behavior parity for pause/resume lifecycle"
      pattern: "status==paused|interrupt_payload|checkpoint_id"
    - from: "src/backend/tests/sdk/test_sdk_async_e2e.py"
      to: "sdk/core/src/agent_search/public_api.py"
      via: "approve/edit/deny/skip end-to-end SDK assertions"
      pattern: "run_async|get_run_status|resume_run"
---

<objective>
Deliver SDK schema and async API parity for subquestion HITL so API and SDK users have equivalent enable/resume capabilities.

Purpose: Satisfy SQH-01 compatibility expectations across SDK surfaces and prevent backend/frontend/SDK contract drift against Plans 02-01 and 02-04 backend behavior.
Output: Updated SDK core schemas/public API/runtime parity and regression tests that prove async HITL behavior through SDK entrypoints.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-subquestion-hitl-end-to-end/02-RESEARCH.md
@.planning/phases/02-subquestion-hitl-end-to-end/02-04-PLAN.md
@sdk/core/src/schemas/agent.py
@sdk/core/src/agent_search/public_api.py
@sdk/core/src/agent_search/runtime/jobs.py
@src/backend/tests/sdk/test_public_api_async.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mirror backend subquestion HITL models into SDK core schemas</name>
  <files>sdk/core/src/schemas/agent.py</files>
  <action>Add SDK schema types equivalent to backend Plan 02-01 contracts: optional subquestion HITL run controls and typed resume decision envelope with approve/edit/deny/skip semantics. Keep defaults additive and backward-compatible so existing SDK calls without HITL fields still validate and run.</action>
  <verify>docker compose exec backend uv run pytest src/backend/tests/sdk/test_public_api_async.py -k "schema or request"</verify>
  <done>SDK schema surface exposes typed HITL config and resume decisions without breaking pre-existing request signatures.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SDK async APIs and runtime jobs for HITL start/resume behavior</name>
  <files>sdk/core/src/agent_search/public_api.py, sdk/core/src/agent_search/runtime/jobs.py</files>
  <action>Update SDK async run and resume pathways to accept and forward typed HITL controls/decisions through existing entrypoints. Maintain compatibility with legacy resume calls, and ensure paused metadata (`interrupt_payload`, `checkpoint_id`) remains accessible to SDK status consumers. Keep endpoint topology unchanged; this is a contract-parity update, not a transport rewrite.</action>
  <verify>docker compose exec backend uv run pytest src/backend/tests/sdk/test_public_api_async.py -k "run_async or resume"</verify>
  <done>SDK users can start HITL-enabled runs and resume with typed decisions through existing public async methods.</done>
</task>

<task type="auto">
  <name>Task 3: Add SDK async regression tests for approve/edit/deny/skip and default-off path</name>
  <files>src/backend/tests/sdk/test_public_api_async.py, src/backend/tests/sdk/test_sdk_async_e2e.py</files>
  <action>Add/extend SDK-focused tests to verify enablement, pause/resume transitions, approve/edit/deny/skip decision effects, and omitted-HITL default behavior. Include negative-path assertions for malformed decision envelopes to confirm typed validation errors are explicit and stable.</action>
  <verify>docker compose exec backend uv run pytest src/backend/tests/sdk/test_public_api_async.py src/backend/tests/sdk/test_sdk_async_e2e.py</verify>
  <done>SDK regression suite proves SQH behavior parity and backward compatibility for async entrypoints.</done>
</task>

</tasks>

<verification>
Run SDK async regression tests via backend pytest targets. Confirm typed SDK models align with backend behavior and that legacy no-HITL invocation paths remain green.
</verification>

<success_criteria>
SDK users can exercise the same subquestion HITL lifecycle as API users (enable, pause, approve/edit/deny/skip resume) while default-off compatibility remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/02-subquestion-hitl-end-to-end/02-subquestion-hitl-end-to-end-03-SUMMARY.md`
</output>
