---
phase: 05-major-release-and-migration-documentation
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - openapi.json
  - sdk/python/README.md
  - docs/application-documentation.html
  - sdk/examples/run_health.py
autonomous: true
must_haves:
  truths:
    - "API/reference documentation reflects current LangGraph runtime-facing interfaces."
    - "Application HTML docs describe the LangGraph architecture and usage model accurately."
    - "Example code can run against a local or remote base URL and demonstrates supported usage."
  artifacts:
    - path: "openapi.json"
      provides: "Canonical API contract used by generated client/reference docs."
      contains: "up-to-date schema from backend app"
    - path: "sdk/python/README.md"
      provides: "Generated client usage docs aligned to current API operations."
      contains: "correct host guidance and endpoint tables"
    - path: "docs/application-documentation.html"
      provides: "Human-readable application architecture docs with LangGraph framing."
      contains: "state graph architecture and migration-aware operational notes"
    - path: "sdk/examples/run_health.py"
      provides: "Runnable SDK example for baseline API connectivity."
      contains: "base URL override and actionable logging"
  key_links:
    - from: "scripts/export_openapi.py"
      to: "openapi.json"
      via: "schema export command"
      pattern: "CANONICAL_OPENAPI_REL_PATH = Path\\(\"openapi\\.json\"\\)"
    - from: "sdk/python/README.md"
      to: "openapi.json"
      via: "generated endpoint/model docs"
      pattern: "API version|Documentation for API Endpoints"
    - from: "sdk/examples/run_health.py"
      to: "/api/health"
      via: "DefaultApi invocation"
      pattern: "health_api_health_get"
---

<objective>
Synchronize OpenAPI-driven references, application HTML docs, and runnable examples with the LangGraph-first architecture.

Purpose: Deliver DOC-03, DOC-04, and DOC-05 while minimizing documentation drift.
Output: Refreshed OpenAPI artifact, aligned SDK/API docs, updated HTML architecture docs, and verified runnable example.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@openapi.json
@scripts/export_openapi.py
@docs/application-documentation.html
@sdk/python/README.md
@sdk/examples/run_health.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate canonical OpenAPI and align generated reference docs</name>
  <files>openapi.json, sdk/python/README.md</files>
  <action>Export OpenAPI from backend into canonical `openapi.json`, regenerate/refresh generated Python SDK documentation as needed, and ensure endpoint/model references in `sdk/python/README.md` match the committed schema. Prefer script-driven regeneration to avoid manual drift.</action>
  <verify>uv run --project src/backend python scripts/export_openapi.py --output openapi.json && ./scripts/validate_openapi.sh</verify>
  <done>Committed OpenAPI matches runtime export and generated reference docs are in sync.</done>
</task>

<task type="auto">
  <name>Task 2: Update application HTML docs for LangGraph architecture correctness</name>
  <files>docs/application-documentation.html</files>
  <action>Revise architecture, runtime flow, and operational sections in the HTML docs to reflect LangGraph-native state graph execution, current migration status, and release-era guidance. Keep this an architecture/content update, not a style redesign.</action>
  <verify>rg "LangGraph|state graph|migration|runtime" docs/application-documentation.html</verify>
  <done>Application docs accurately describe the migrated architecture and operational behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh runnable example for LangGraph-era docs</name>
  <files>sdk/examples/run_health.py</files>
  <action>Ensure `run_health.py` remains executable as a baseline SDK example with clear base URL selection and failure logging. If needed, add concise usage notes/comments for remote target checks without changing endpoint semantics.</action>
  <verify>python sdk/examples/run_health.py --base-url http://localhost:8000</verify>
  <done>Example runs successfully against a healthy service and remains aligned with documented SDK flow.</done>
</task>

</tasks>

<verification>
- `uv run --project src/backend python scripts/export_openapi.py --output openapi.json`
- `./scripts/validate_openapi.sh`
- `python sdk/examples/run_health.py --base-url http://localhost:8000`
- Open `docs/application-documentation.html` and confirm LangGraph architecture language appears in overview/flow sections.
</verification>

<success_criteria>
- OpenAPI, generated SDK reference docs, and application HTML docs are mutually consistent.
- Docs under `docs/` clearly describe LangGraph architecture and migration-ready usage.
- Example script runs and demonstrates expected API connectivity behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/05-major-release-and-migration-documentation/05-03-SUMMARY.md`
</output>
