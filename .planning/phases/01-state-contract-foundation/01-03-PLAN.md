---
phase: 01-state-contract-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/backend/agent_search/runtime/reducers.py
  - src/backend/services/agent_service.py
  - src/backend/tests/services/test_agent_service.py
  - src/backend/tests/sdk/test_runtime_reducers.py
  - docs/langgraph-reducer-semantics.md
autonomous: true
must_haves:
  truths:
    - "Equivalent graph transitions always produce the same merged state for the same input sequence."
    - "Parallel lane updates resolve predictably because reducer behavior is explicit per state channel."
    - "Reducer semantics are documented and can be verified through repeat-run assertions."
  artifacts:
    - path: "src/backend/agent_search/runtime/reducers.py"
      provides: "Explicit per-key reducer functions used for graph-state merge semantics."
      exports: ["merge_decomposition_sub_questions", "merge_sub_question_artifacts", "merge_citation_rows_by_index", "merge_sub_qa", "merge_stage_snapshots"]
    - path: "src/backend/tests/sdk/test_runtime_reducers.py"
      provides: "Reducer unit tests for deterministic merge behavior and edge cases."
      min_lines: 40
    - path: "docs/langgraph-reducer-semantics.md"
      provides: "Reference semantics for each merge channel and ordering guarantees."
      min_lines: 35
  key_links:
    - from: "src/backend/services/agent_service.py"
      to: "src/backend/agent_search/runtime/reducers.py"
      via: "state transition functions call shared reducers instead of implicit merge logic"
      pattern: "merge_"
    - from: "src/backend/tests/services/test_agent_service.py"
      to: "src/backend/services/agent_service.py"
      via: "repeat-run deterministic transition assertions"
      pattern: "(parallel|sequential|deterministic)"
    - from: "docs/langgraph-reducer-semantics.md"
      to: "src/backend/agent_search/runtime/reducers.py"
      via: "documentation mirrors reducer behavior"
      pattern: "(append|replace|dedupe|stable-order)"
---

<objective>
Make graph transition and merge behavior deterministic by introducing explicit reducer semantics per state channel and validating them with repeat-run tests.

Purpose: Remove hidden merge behavior and ensure reliable, reproducible outcomes under parallel and sequential execution paths.
Output: Shared reducer module, wired transition logic, deterministic tests, and reducer semantics documentation.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/backend/services/agent_service.py
@src/backend/agent_search/runtime/runner.py
@src/backend/tests/services/test_agent_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement explicit reducer semantics for state channels</name>
  <files>src/backend/agent_search/runtime/reducers.py, src/backend/services/agent_service.py</files>
  <action>Create reducer functions for each merge-sensitive state channel (sub-question lists, artifact collections, citation index maps, sub_qa entries, and stage snapshots). Define deterministic ordering and conflict resolution rules (for example: stable insertion order, deterministic overwrite by channel-specific key, no nondeterministic dict/list merges). Replace ad-hoc merge/update logic in service transition helpers with calls to these reducers.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/services/test_agent_service.py -k "apply_ or parallel_graph_runner or sequential_graph_runner"`.</verify>
  <done>State transition code delegates merge behavior to explicit reducer functions and no channel relies on implicit Python merge side effects.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic repeat-run tests and reducer semantics documentation</name>
  <files>src/backend/tests/sdk/test_runtime_reducers.py, src/backend/tests/services/test_agent_service.py, docs/langgraph-reducer-semantics.md</files>
  <action>Add reducer unit tests that run identical merge inputs multiple times and assert byte-for-byte equivalent outputs for each channel. Extend service-level tests to assert repeated parallel/serial transition runs produce equivalent merged state shape/content. Document each reducer channel with semantics (append, replace, dedupe, ordering) and include examples aligned to tests. Do not leave semantics implicit in prose-only comments.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/sdk/test_runtime_reducers.py src/backend/tests/services/test_agent_service.py -k "deterministic or reducer or graph_runner"`.</verify>
  <done>Reducer behavior is test-proven deterministic and documented with channel-level semantics that match implementation and tests.</done>
</task>

</tasks>

<verification>
Run `docker compose exec backend uv run pytest src/backend/tests/sdk/test_runtime_reducers.py src/backend/tests/services/test_agent_service.py -k "deterministic or reducer or graph_runner"` and confirm repeated transition runs produce identical merge outcomes.
</verification>

<success_criteria>
Graph transition/merge behavior is explicitly reducer-driven, documented, and validated by repeat-run deterministic tests across unit and service layers.
</success_criteria>

<output>
After completion, create `.planning/phases/01-state-contract-foundation/01-03-SUMMARY.md`
</output>
