---
phase: 01-state-contract-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/agent_search/runtime/node_contracts.py
  - src/backend/tests/sdk/test_node_contract_registry.py
  - docs/langgraph-node-io-contracts.md
autonomous: true
must_haves:
  truths:
    - "Engineers can identify every graph node input and output schema directly from code."
    - "Engineers can find the same node input/output mapping in reference docs without reverse-engineering implementation details."
    - "Code and docs contract mapping stay synchronized through automated checks."
  artifacts:
    - path: "src/backend/agent_search/runtime/node_contracts.py"
      provides: "Single registry mapping each node to explicit input/output schema classes."
      exports: ["NODE_IO_CONTRACTS", "iter_node_io_contracts"]
    - path: "docs/langgraph-node-io-contracts.md"
      provides: "Reference document listing all graph nodes with explicit input/output schema contracts."
      min_lines: 40
    - path: "src/backend/tests/sdk/test_node_contract_registry.py"
      provides: "Tests ensuring registry completeness and docs parity checks."
      min_lines: 30
  key_links:
    - from: "src/backend/agent_search/runtime/node_contracts.py"
      to: "src/backend/schemas/agent.py"
      via: "node contract schema class references"
      pattern: "(DecomposeNodeInput|ExpandNodeInput|SearchNodeInput|RerankNodeInput|AnswerSubquestionNodeInput|SynthesizeFinalNodeInput)"
    - from: "docs/langgraph-node-io-contracts.md"
      to: "src/backend/agent_search/runtime/node_contracts.py"
      via: "doc table generated from or validated against registry"
      pattern: "(Decompose|Expand|Search|Rerank|Answer|Synthesize)"
---

<objective>
Formalize explicit graph node input/output schema contracts in both code and docs so node interfaces are discoverable and auditable.

Purpose: Prevent implicit node-interface drift and make contract inspection deterministic for maintainers and integrators.
Output: A code-first node contract registry plus synchronized reference documentation and contract-parity tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/backend/schemas/agent.py
@src/backend/agent_search/runtime/nodes/__init__.py
@src/backend/agent_search/runtime/nodes/decompose.py
@src/backend/agent_search/runtime/nodes/expand.py
@src/backend/agent_search/runtime/nodes/search.py
@src/backend/agent_search/runtime/nodes/rerank.py
@src/backend/agent_search/runtime/nodes/answer.py
@src/backend/agent_search/runtime/nodes/synthesize.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a canonical node I/O contract registry in runtime code</name>
  <files>src/backend/agent_search/runtime/node_contracts.py</files>
  <action>Create a single authoritative registry that maps each runtime node (`decompose`, `expand`, `search`, `rerank`, `answer_subquestion`, `synthesize_final`) to explicit input/output schema classes from `schemas/agent.py`. Include helper APIs to iterate contracts in stable order for tests/docs. Avoid ad-hoc hardcoded lists in multiple places; this file must be the source of truth.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/sdk/test_node_contract_registry.py -k registry` after adding tests in Task 2.</verify>
  <done>All graph nodes are represented in one code-level contract mapping with explicit schema types and stable lookup semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Publish node contract reference docs and enforce parity checks</name>
  <files>docs/langgraph-node-io-contracts.md, src/backend/tests/sdk/test_node_contract_registry.py</files>
  <action>Create reference docs that list node name, input schema, output schema, and implementation entrypoint for each graph node. Add tests that fail when registry coverage or docs entries diverge (for example, missing node or mismatched schema name). Keep docs concise and contract-focused; do not include operational behavior covered by other docs.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/sdk/test_node_contract_registry.py`.</verify>
  <done>Node I/O schemas are discoverable from both code and docs, and automated checks detect mapping drift.</done>
</task>

</tasks>

<verification>
Run `docker compose exec backend uv run pytest src/backend/tests/sdk/test_node_contract_registry.py` and confirm every runtime node has an explicit input/output contract and docs parity assertions pass.
</verification>

<success_criteria>
Every graph node contract is explicit, centrally indexed, and reflected in docs with automated parity checks to catch drift.
</success_criteria>

<output>
After completion, create `.planning/phases/01-state-contract-foundation/01-02-SUMMARY.md`
</output>
