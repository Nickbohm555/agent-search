---
phase: 01-state-contract-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/agent_search/runtime/state.py
  - src/backend/agent_search/runtime/__init__.py
  - src/backend/agent_search/__init__.py
  - src/backend/schemas/agent.py
  - src/backend/schemas/__init__.py
  - src/backend/tests/sdk/test_rag_state_contract.py
autonomous: true
must_haves:
  truths:
    - "SDK consumers can import a canonical typed RAGState from the public package without using internal schemas."
    - "Runtime state can be represented as a single canonical contract that captures all graph channels used during execution."
    - "Boundary models remain validated while internal graph state can flow through typed state channels."
  artifacts:
    - path: "src/backend/agent_search/runtime/state.py"
      provides: "Canonical RAGState TypedDict contract plus adapters between boundary models and runtime state."
      exports: ["RAGState", "to_rag_state", "from_rag_state"]
    - path: "src/backend/agent_search/__init__.py"
      provides: "Public SDK export surface for canonical state contract."
      contains: "__all__ includes RAGState"
    - path: "src/backend/tests/sdk/test_rag_state_contract.py"
      provides: "Import and contract-shape assertions for SDK consumers."
      min_lines: 30
  key_links:
    - from: "src/backend/agent_search/__init__.py"
      to: "src/backend/agent_search/runtime/state.py"
      via: "public re-export"
      pattern: "from \\.runtime\\.state import RAGState"
    - from: "src/backend/services/agent_service.py"
      to: "src/backend/agent_search/runtime/state.py"
      via: "state conversion helpers used at graph boundaries"
      pattern: "(to_rag_state|from_rag_state)"
---

<objective>
Establish a canonical typed `RAGState` contract that becomes the single source of truth for runtime graph state and is directly importable by SDK consumers.

Purpose: Eliminate state-contract drift between internal runtime state handling and external SDK consumers.
Output: A public `RAGState` contract with conversion helpers and SDK-facing import guarantees.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/backend/schemas/agent.py
@src/backend/schemas/__init__.py
@src/backend/agent_search/__init__.py
@src/backend/services/agent_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical runtime state contract with boundary adapters</name>
  <files>src/backend/agent_search/runtime/state.py, src/backend/schemas/agent.py, src/backend/schemas/__init__.py</files>
  <action>Create `RAGState` in `src/backend/agent_search/runtime/state.py` using `TypedDict` and `Annotated` channel types for fields that participate in parallel graph updates. Keep existing Pydantic node input/output models as boundary contracts, and add explicit adapter helpers (for example `to_rag_state` and `from_rag_state`) so runtime internals can consume canonical state while API/service boundaries still use validated models. Do not duplicate a second competing state shape; map all existing graph channels to this one contract.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/services/test_agent_service.py -k "graph_state or map_graph_state"` and confirm no regressions in existing state mapping behavior.</verify>
  <done>Canonical `RAGState` exists as the only named runtime state contract and boundary conversion functions preserve current graph payload compatibility.</done>
</task>

<task type="auto">
  <name>Task 2: Export canonical state through SDK surface and validate consumer imports</name>
  <files>src/backend/agent_search/runtime/__init__.py, src/backend/agent_search/__init__.py, src/backend/tests/sdk/test_rag_state_contract.py</files>
  <action>Re-export `RAGState` through runtime and top-level SDK package modules so consumers can import from `agent_search` without reaching into `schemas`. Add focused SDK tests proving import path stability, required key presence, and compatibility with existing runtime request/response usage. Keep tests consumer-oriented (import and type contract), not implementation-internal.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/sdk/test_rag_state_contract.py`.</verify>
  <done>SDK consumers can import `RAGState` from public package exports and tests assert contract availability without private-module shims.</done>
</task>

</tasks>

<verification>
Run `docker compose exec backend uv run pytest src/backend/tests/sdk/test_rag_state_contract.py src/backend/tests/services/test_agent_service.py -k "graph_state or map_graph_state"` to confirm canonical state contract integration and existing graph mapping stability.
</verification>

<success_criteria>
`RAGState` is a canonical typed contract used for runtime state representation, exported from SDK public entrypoints, and validated by automated tests proving importability and boundary compatibility.
</success_criteria>

<output>
After completion, create `.planning/phases/01-state-contract-foundation/01-01-SUMMARY.md`
</output>
