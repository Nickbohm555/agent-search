---
phase: 06-sdk-contract-parity-and-pypi-release
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - sdk/core/pyproject.toml
  - scripts/release_sdk.sh
  - .github/workflows/release-sdk.yml
autonomous: true
must_haves:
  truths:
    - "A new `agent-search-core` version can be built, validated, and published through the repository release path without manual artifact drift."
    - "Release execution fails fast when tag/version alignment is wrong, preventing accidental publication under mismatched metadata."
    - "Published artifact flow uses build-once/publish-once behavior tied to validated distribution outputs."
  artifacts:
    - path: "sdk/core/pyproject.toml"
      provides: "Version bump for the release candidate package to be published."
    - path: "scripts/release_sdk.sh"
      provides: "Deterministic local/CI build and package validation gate for release artifacts."
    - path: ".github/workflows/release-sdk.yml"
      provides: "Trusted publishing CI flow that publishes only validated distribution artifacts."
  key_links:
    - from: "sdk/core/pyproject.toml"
      to: "scripts/release_sdk.sh"
      via: "release script reads package version and enforces expected tag naming."
      pattern: "version =|agent-search-core-v"
    - from: "scripts/release_sdk.sh"
      to: ".github/workflows/release-sdk.yml"
      via: "workflow build job executes release script before artifact upload/publish."
      pattern: "Build and validate SDK artifacts|Publish to PyPI"
---

<objective>
Execute REL-04 release hardening so `agent-search-core` can be published from a parity-validated, reproducible pipeline.

Purpose: After contract parity, release publication must be safe, deterministic, and installable for external consumers.
Output: Version bump, release script/workflow guard updates, and validated publish flow instructions ready for execution.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sdk-contract-parity-and-pypi-release/06-RESEARCH.md
@.planning/phases/06-sdk-contract-parity-and-pypi-release/06-01-SUMMARY.md
@sdk/core/pyproject.toml
@scripts/release_sdk.sh
@.github/workflows/release-sdk.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut release version and enforce tag/version gating</name>
  <files>sdk/core/pyproject.toml, scripts/release_sdk.sh</files>
  <action>Bump `agent-search-core` to the next release version in `sdk/core/pyproject.toml` and keep `scripts/release_sdk.sh` as the canonical gate that rejects mismatched `RELEASE_TAG` values. If needed, tighten script checks so both dry-run and publish mode prove artifacts are built from the declared version only. Do not introduce alternate publish scripts or bypass paths.</action>
  <verify>Run `./scripts/release_sdk.sh` and confirm successful build/check output; then run with an intentionally wrong `RELEASE_TAG` and confirm it fails with tag mismatch.</verify>
  <done>Release version is updated and release script enforces strict metadata alignment before any upload path can run.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure CI Trusted Publishing publishes validated artifacts only</name>
  <files>.github/workflows/release-sdk.yml, scripts/release_sdk.sh</files>
  <action>Align release workflow steps so build/check happens before publish, publish consumes uploaded artifacts (not rebuilt artifacts), and workflow dispatch behavior matches tag-trigger behavior. Keep Trusted Publishing (`id-token: write`, `pypa/gh-action-pypi-publish`) as the only CI publish mechanism. Preserve optional non-publish dry runs for preflight validation.</action>
  <verify>Run `rg -n "id-token: write|upload-artifact|download-artifact|gh-action-pypi-publish|Build and validate SDK artifacts" .github/workflows/release-sdk.yml` and run `./scripts/release_sdk.sh` to confirm local preflight still passes.</verify>
  <done>Workflow and script together enforce build-once/publish-once release semantics for REL-04 publication.</done>
</task>

<task type="auto">
  <name>Task 3: Validate published version is installable by consumers</name>
  <files>sdk/core/pyproject.toml, .github/workflows/release-sdk.yml</files>
  <action>Execute an installability proof after publish using the newly cut version (for example ephemeral venv install and import check) and make it part of release execution evidence. Keep this task focused on proving consumer installation success, not on additional contract implementation changes.</action>
  <verify>After publish, run `python3 -m venv /tmp/agent-search-core-release-check && source /tmp/agent-search-core-release-check/bin/activate && pip install --upgrade pip && pip install agent-search-core==<new_version> && python -c "import agent_search; print(agent_search.__file__)"`.</verify>
  <done>Newly published package version installs successfully from PyPI and imports in a clean environment.</done>
</task>

</tasks>

<verification>
Release is complete only when package versioning, CI publish flow, and real-world installability all validate the same published artifact.
</verification>

<success_criteria>
REL-04 is satisfied: a new `agent-search-core` version is published and verifiably installable via standard consumer tooling.
</success_criteria>

<output>
After completion, create `.planning/phases/06-sdk-contract-parity-and-pypi-release/06-02-SUMMARY.md`
</output>
