---
phase: 06-sdk-contract-parity-and-pypi-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/schemas/agent.py
  - src/backend/routers/agent.py
  - src/backend/agent_search/config.py
  - sdk/core/src/schemas/agent.py
  - sdk/core/src/agent_search/config.py
  - openapi.json
  - sdk/python/openapi_client/models/runtime_agent_run_request.py
  - sdk/python/openapi_client/models/runtime_agent_run_response.py
  - src/backend/tests/api/test_agent_run.py
  - src/backend/tests/sdk/test_runtime_config.py
autonomous: true
must_haves:
  truths:
    - "SDK and OpenAPI request contracts accept HITL controls and prompt options using the same external field shapes as backend run endpoints."
    - "SDK and OpenAPI response contracts expose additive `sub_answers` compatibility without removing existing required fields."
    - "Contract parity gates prove generated artifacts are in sync with runtime-exported OpenAPI before release."
  artifacts:
    - path: "src/backend/schemas/agent.py"
      provides: "Canonical request/response schema fields and aliases for HITL, controls, prompt options, and additive sub-answer output."
    - path: "sdk/core/src/schemas/agent.py"
      provides: "In-process SDK model parity with backend request/response contracts."
    - path: "openapi.json"
      provides: "Committed OpenAPI contract that includes all Phase 1-5 additive fields used by external consumers."
    - path: "sdk/python/openapi_client/models/runtime_agent_run_request.py"
      provides: "Generated HTTP SDK request model parity for run controls and prompt options."
    - path: "sdk/python/openapi_client/models/runtime_agent_run_response.py"
      provides: "Generated HTTP SDK response model parity including additive sub-answer field support."
  key_links:
    - from: "src/backend/schemas/agent.py"
      to: "openapi.json"
      via: "OpenAPI export captures canonical backend contract for external SDK generation."
      pattern: "runtime_config|custom_prompts|sub_answers|sub_qa"
    - from: "openapi.json"
      to: "sdk/python/openapi_client/models/runtime_agent_run_request.py"
      via: "OpenAPI Generator refresh creates request models matching backend contract."
      pattern: "RuntimeAgentRunRequest"
    - from: "openapi.json"
      to: "sdk/python/openapi_client/models/runtime_agent_run_response.py"
      via: "OpenAPI Generator refresh creates response models with additive compatibility fields."
      pattern: "RuntimeAgentRunResponse"
---

<objective>
Finalize REL-03 contract parity across backend schemas, committed OpenAPI, in-process SDK models, and generated HTTP SDK models.

Purpose: Phase 6 cannot publish safely until external SDK artifacts exactly mirror the additive Phase 1-5 contract surface.
Output: Updated backend/SDK schema surfaces, refreshed `openapi.json`, regenerated `sdk/python` models, and parity regression tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sdk-contract-parity-and-pypi-release/06-RESEARCH.md
@src/backend/schemas/agent.py
@src/backend/routers/agent.py
@src/backend/agent_search/config.py
@sdk/core/src/schemas/agent.py
@sdk/core/src/agent_search/config.py
@scripts/update_sdk.sh
@scripts/validate_openapi.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock canonical run contract fields and additive alias compatibility</name>
  <files>src/backend/schemas/agent.py, src/backend/routers/agent.py, src/backend/agent_search/config.py, sdk/core/src/schemas/agent.py, sdk/core/src/agent_search/config.py</files>
  <action>Update backend and in-process SDK request/response models so run payloads and outputs include the Phase 1-5 contract surface (`runtime_config` controls, HITL options, custom prompt options, and additive `sub_answers` compatibility alongside legacy `sub_qa`). Keep legacy-safe defaults: omitted new fields must preserve existing behavior. Keep canonical external naming consistent and explicitly accept documented aliases where compatibility requires them.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/api/test_agent_run.py src/backend/tests/sdk/test_runtime_config.py -k "runtime_config or hitl or prompt or sub_answers or sub_qa"`.</verify>
  <done>Backend and sdk/core contract models expose all REL-03 fields with compatibility-safe defaults and additive alias behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate OpenAPI and generated HTTP SDK artifacts atomically</name>
  <files>openapi.json, sdk/python/openapi_client/models/runtime_agent_run_request.py, sdk/python/openapi_client/models/runtime_agent_run_response.py</files>
  <action>Run the repository regeneration flow (`./scripts/update_sdk.sh`) after schema updates and commit all contract artifacts produced by that run. Do not hand-edit generated files. Ensure request and response generated models both contain the same control/HITL/prompt/sub-answer fields exposed in backend schema definitions.</action>
  <verify>Run `./scripts/update_sdk.sh && ./scripts/validate_openapi.sh` and confirm no drift output.</verify>
  <done>`openapi.json` and generated `sdk/python` models are refreshed and in lockstep with runtime-exported backend contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Add release-blocking parity regressions for new external contract fields</name>
  <files>src/backend/tests/api/test_agent_run.py, src/backend/tests/sdk/test_runtime_config.py</files>
  <action>Add focused regression tests for contract acceptance and defaults: payloads with new HITL/control/prompt fields validate, payloads omitting them preserve legacy behavior, and response serialization exposes additive sub-answer compatibility. Keep tests at API/config boundary level; release publishing and docs validation are handled in later plans.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/api/test_agent_run.py src/backend/tests/sdk/test_runtime_config.py` and `./scripts/validate_openapi.sh`.</verify>
  <done>Tests explicitly protect REL-03 contract parity and default-safe compatibility ahead of release publication.</done>
</task>

</tasks>

<verification>
Contract parity is complete only when backend schemas, sdk/core models, committed OpenAPI, generated SDK models, and drift gates all agree on HITL/control/prompt/sub-answer surfaces.
</verification>

<success_criteria>
REL-03 is satisfied: external SDK/OpenAPI surfaces now match backend contracts for HITL, controls, prompt options, and additive sub-answer output.
</success_criteria>

<output>
After completion, create `.planning/phases/06-sdk-contract-parity-and-pypi-release/06-01-SUMMARY.md`
</output>
