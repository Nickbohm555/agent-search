---
phase: 04-operator-controls-and-result-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/schemas/agent.py
  - src/backend/routers/agent.py
  - src/backend/agent_search/public_api.py
  - src/backend/tests/api/test_agent_run.py
  - src/backend/tests/sdk/test_public_api.py
  - src/backend/tests/sdk/test_public_api_async.py
autonomous: true
must_haves:
  truths:
    - "Run endpoints accept per-run retrieval control input without requiring frontend-specific fields."
    - "SDK sync and async entrypoints accept rerank/query-expansion controls through config and preserve thread_id behavior."
    - "Contract-level tests prove the new control path is additive and compatible."
  artifacts:
    - path: "src/backend/schemas/agent.py"
      provides: "Typed request contract with runtime_config fields for rerank/query_expansion controls."
    - path: "src/backend/routers/agent.py"
      provides: "Router mapping from request payload to SDK config payload including runtime_config."
    - path: "src/backend/agent_search/public_api.py"
      provides: "SDK-level config parsing and propagation for run and run_async."
    - path: "src/backend/tests/api/test_agent_run.py"
      provides: "API contract tests for runtime_config forwarding and backward compatibility."
    - path: "src/backend/tests/sdk/test_public_api.py"
      provides: "SDK sync tests proving runtime_config acceptance and forwarding."
    - path: "src/backend/tests/sdk/test_public_api_async.py"
      provides: "SDK async tests proving runtime_config acceptance and forwarding."
  key_links:
    - from: "src/backend/schemas/agent.py"
      to: "src/backend/routers/agent.py"
      via: "RuntimeAgentRunRequest.runtime_config model field consumed by router handlers"
      pattern: "runtime_config"
    - from: "src/backend/routers/agent.py"
      to: "src/backend/agent_search/public_api.py"
      via: "router builds SDK config containing runtime_config and passes to run APIs"
      pattern: "config=.*runtime_config"
    - from: "src/backend/tests/api/test_agent_run.py"
      to: "src/backend/routers/agent.py"
      via: "assert captured config includes thread_id + runtime_config"
      pattern: "captured\\[\"config\"\\]"
---

<objective>
Establish the canonical run-options contract for Phase 4 by adding `runtime_config` to run request surfaces and plumbing it from API payloads to SDK entrypoints.

Purpose: This unlocks CTRL-01 and CTRL-03 by making retrieval controls first-class, per-run parameters rather than UI-only or env-only toggles.
Output: Backward-compatible request models, router-to-SDK forwarding, and contract tests that pin the new behavior.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-operator-controls-and-result-visibility/04-RESEARCH.md
@src/backend/schemas/agent.py
@src/backend/routers/agent.py
@src/backend/agent_search/public_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add additive runtime_config request contract</name>
  <files>src/backend/schemas/agent.py</files>
  <action>Add a typed `runtime_config` field to `RuntimeAgentRunRequest` that can carry `rerank` and `query_expansion` options. Keep this additive and optional so existing requests with only `query` and optional `thread_id` remain valid. Do not rename existing `sub_qa` response fields in this task.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/api/test_agent_run.py -k "thread_id or run"` and confirm request validation remains backward compatible while accepting `runtime_config`.</verify>
  <done>`RuntimeAgentRunRequest` accepts optional `runtime_config` and existing callsites/tests that omit it still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Forward runtime_config through router and SDK entrypoints</name>
  <files>src/backend/routers/agent.py, src/backend/agent_search/public_api.py</files>
  <action>Update router config-building logic to include both `thread_id` and `runtime_config` when present, then ensure `advanced_rag` and `run_async` preserve and parse this nested config path. Maintain SDK interface shape (`config` dict) so callers remain decoupled from frontend toggles. Avoid introducing UI terminology into SDK/public API modules.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/sdk/test_public_api.py src/backend/tests/sdk/test_public_api_async.py` and confirm config forwarding assertions include runtime_config while existing signatures remain unchanged.</verify>
  <done>Router passes `runtime_config` to SDK config, SDK accepts it in sync/async flows, and thread_id lineage behavior remains unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add API and SDK regression coverage for additive contract</name>
  <files>src/backend/tests/api/test_agent_run.py, src/backend/tests/sdk/test_public_api.py, src/backend/tests/sdk/test_public_api_async.py</files>
  <action>Add focused tests that send `runtime_config.rerank.enabled` and `runtime_config.query_expansion.enabled` through API and SDK surfaces, asserting forwarding behavior and no regression for legacy payloads lacking `runtime_config`. Keep tests narrow to contract plumbing (not runtime algorithm behavior; that is handled in the next plan).</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/api/test_agent_run.py src/backend/tests/sdk/test_public_api.py src/backend/tests/sdk/test_public_api_async.py`.</verify>
  <done>Tests prove additive compatibility and control-field forwarding across API + SDK sync/async entrypoints.</done>
</task>

</tasks>

<verification>
`runtime_config` is optional and additive; API/SDK tests pass with and without it, and config forwarding includes both `thread_id` and nested retrieval controls.
</verification>

<success_criteria>
CTRL-03 contract foundation is in place: SDK users can pass rerank/query-expansion controls as run parameters, and API requests can carry the same structure without breaking existing clients.
</success_criteria>

<output>
After completion, create `.planning/phases/04-operator-controls-and-result-visibility/04-01-SUMMARY.md`
</output>
