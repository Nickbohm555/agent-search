---
phase: 04-operator-controls-and-result-visibility
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/backend/agent_search/config.py
  - src/backend/services/agent_service.py
  - src/backend/tests/sdk/test_runtime_config.py
  - src/backend/tests/services/test_agent_service.py
autonomous: true
must_haves:
  truths:
    - "Per-run `query_expansion.enabled` changes runtime behavior without needing env changes."
    - "Per-run `rerank.enabled` changes runtime behavior without changing global process config."
    - "Default behavior remains unchanged when runtime_config is absent."
  artifacts:
    - path: "src/backend/agent_search/config.py"
      provides: "RuntimeConfig support for query_expansion controls alongside existing rerank controls."
    - path: "src/backend/services/agent_service.py"
      provides: "Effective config resolution path from run payload to expand/rerank node execution."
    - path: "src/backend/tests/sdk/test_runtime_config.py"
      provides: "Unit tests for defaults and overrides including query_expansion settings."
    - path: "src/backend/tests/services/test_agent_service.py"
      provides: "Behavior tests confirming runtime node execution respects per-run controls."
  key_links:
    - from: "src/backend/agent_search/config.py"
      to: "src/backend/services/agent_service.py"
      via: "RuntimeConfig parsed from request config and converted into node-level configs"
      pattern: "RuntimeConfig\\.from_dict|query_expansion|rerank"
    - from: "src/backend/services/agent_service.py"
      to: "services.query_expansion_service"
      via: "expand node receives effective config derived from runtime_config"
      pattern: "run_expand_node|QueryExpansionConfig"
    - from: "src/backend/services/agent_service.py"
      to: "services.reranker_service"
      via: "rerank node receives effective config derived from runtime_config"
      pattern: "run_rerank_node|rerank_documents"
---

<objective>
Make runtime controls operational by applying request-scoped `runtime_config` values to expand/rerank execution in the runtime pipeline.

Purpose: CTRL-01 and CTRL-03 require controls to affect real retrieval behavior, not just payload acceptance.
Output: Runtime config model + service wiring + regression tests that verify default/off/override behavior.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-operator-controls-and-result-visibility/04-RESEARCH.md
@.planning/phases/04-operator-controls-and-result-visibility/04-01-SUMMARY.md
@src/backend/agent_search/config.py
@src/backend/services/agent_service.py
@src/backend/tests/sdk/test_runtime_config.py
@src/backend/tests/services/test_agent_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend RuntimeConfig to include query expansion section</name>
  <files>src/backend/agent_search/config.py, src/backend/tests/sdk/test_runtime_config.py</files>
  <action>Add a `query_expansion` section to `RuntimeConfig` with at least an `enabled` flag and safe defaults aligned with current behavior. Ensure invalid values fall back to defaults consistently with existing parser style. Add unit tests for default value, explicit disable, and invalid override fallback.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/sdk/test_runtime_config.py`.</verify>
  <done>`RuntimeConfig.from_dict` returns stable defaults and supports `query_expansion.enabled` overrides with tested fallback behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Apply per-run config in expand and rerank runtime execution</name>
  <files>src/backend/services/agent_service.py</files>
  <action>Thread parsed runtime config through the runtime service path and resolve effective expand/rerank config per run. Replace env-only assumptions where needed so expand/rerank node calls use request-derived settings when provided, while preserving existing defaults when omitted. Keep changes scoped to config selection/wiring; avoid unrelated pipeline refactors.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/services/test_agent_service.py -k "expand or rerank or runtime_config"`.</verify>
  <done>Expand/rerank execution uses per-run controls when supplied and preserves current behavior when absent.</done>
</task>

<task type="auto">
  <name>Task 3: Add runtime behavior regression tests for control toggles</name>
  <files>src/backend/tests/services/test_agent_service.py</files>
  <action>Add focused tests proving (a) query expansion can be disabled per run and (b) rerank can be disabled per run without mutating global defaults. Assert outcomes via node-call behavior or emitted artifacts/provenance, not just object construction.</action>
  <verify>Run `docker compose exec backend uv run pytest src/backend/tests/services/test_agent_service.py`.</verify>
  <done>Service-level tests demonstrate real runtime behavior changes for per-run control toggles.</done>
</task>

</tasks>

<verification>
Per-run control flags alter expand/rerank behavior in runtime execution, with defaults preserved and covered by deterministic tests.
</verification>

<success_criteria>
CTRL-03 behavioral parity is complete: SDK/API control parameters meaningfully affect runtime expand/rerank behavior, not just schema acceptance.
</success_criteria>

<output>
After completion, create `.planning/phases/04-operator-controls-and-result-visibility/04-02-SUMMARY.md`
</output>
