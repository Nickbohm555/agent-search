---
phase: 04-operator-controls-and-result-visibility
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/frontend/src/utils/api.ts
  - src/frontend/src/App.tsx
  - src/frontend/src/App.test.tsx
autonomous: true
must_haves:
  truths:
    - "Frontend users can independently toggle rerank and query expansion before starting a run."
    - "Run start requests include these toggles as backend runtime parameters (not UI-specific names)."
    - "Run result views keep showing sub-answer output from streamed/final payloads and remain stable with contract evolution."
  artifacts:
    - path: "src/frontend/src/App.tsx"
      provides: "Run form controls for rerank/query expansion and wiring to startAgentRun payload."
    - path: "src/frontend/src/utils/api.ts"
      provides: "Typed run request payload support for runtime_config and resilient sub-answer response parsing."
    - path: "src/frontend/src/App.test.tsx"
      provides: "UI/contract tests for toggle payloads and sub-answer rendering continuity."
  key_links:
    - from: "src/frontend/src/App.tsx"
      to: "src/frontend/src/utils/api.ts"
      via: "submit handler maps UI booleans to runtime_config.rerank.enabled and runtime_config.query_expansion.enabled"
      pattern: "startAgentRun\\(.*runtimeConfig"
    - from: "src/frontend/src/utils/api.ts"
      to: "/api/agents/run-async"
      via: "POST payload includes runtime_config shape expected by backend"
      pattern: "runtime_config"
    - from: "src/frontend/src/utils/api.ts"
      to: "src/frontend/src/App.tsx"
      via: "response validators keep sub-answer data available for UI panels and summaries"
      pattern: "sub_qa|sub_answers"
---

<objective>
Deliver Phase 4 operator controls in the frontend and preserve visible sub-answer output in run results under the updated contract.

Purpose: This satisfies CTRL-01 and REL-02 from the user-facing surface by ensuring controls are usable and sub-answer visibility is reliable.
Output: UI toggles mapped to run parameters, API typing updates, and frontend tests covering request payload + sub-answer rendering.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-operator-controls-and-result-visibility/04-RESEARCH.md
@.planning/phases/04-operator-controls-and-result-visibility/04-01-SUMMARY.md
@src/frontend/src/utils/api.ts
@src/frontend/src/App.tsx
@src/frontend/src/App.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add frontend run controls and payload mapping</name>
  <files>src/frontend/src/App.tsx, src/frontend/src/utils/api.ts</files>
  <action>Add two independent run-start controls in the app UI for rerank and query expansion. Update `startAgentRun` to accept optional runtime options and serialize them to `runtime_config` in `/api/agents/run-async` requests. Keep control labels UX-facing, but map to backend canonical fields (`rerank.enabled`, `query_expansion.enabled`) without inventing new API keys.</action>
  <verify>Run `docker compose exec frontend npm run test -- App.test.tsx` and confirm request body assertions include `runtime_config` with independent toggle values.</verify>
  <done>Users can set rerank/query-expansion per run from UI and run-start payload contains correctly mapped runtime_config fields.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve and normalize sub-answer visibility through response parsing</name>
  <files>src/frontend/src/utils/api.ts, src/frontend/src/App.tsx</files>
  <action>Ensure response/event parsing continues to surface sub-answer data in all relevant run views. If backend provides additive `sub_answers`, normalize it alongside existing `sub_qa` so rendering remains stable during contract transition. Do not remove current `sub_qa` support in this phase.</action>
  <verify>Run `docker compose exec frontend npm run test -- App.test.tsx -t "subanswer|run query flow|SSE"`.</verify>
  <done>Sub-answer panels and summaries continue rendering from streamed/final payloads with no regression during contract evolution.</done>
</task>

<task type="auto">
  <name>Task 3: Add frontend regression tests for controls and sub-answer rendering</name>
  <files>src/frontend/src/App.test.tsx</files>
  <action>Add tests covering: (1) rerank/query-expansion control defaults and independent toggling, (2) request payload shape containing `runtime_config`, and (3) retained sub-answer rendering for streamed and final results. Keep fixtures aligned with existing fake EventSource testing pattern.</action>
  <verify>Run `docker compose exec frontend npm run test -- App.test.tsx`.</verify>
  <done>Frontend tests prove control UX behavior and REL-02 sub-answer visibility are preserved.</done>
</task>

</tasks>

<verification>
Frontend run form emits runtime_config controls correctly and run results keep visible sub-answer output under streamed and terminal events.
</verification>

<success_criteria>
CTRL-01 and REL-02 are met on the frontend: users can configure rerank/query expansion at run start and can consistently view sub-answer outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/04-operator-controls-and-result-visibility/04-03-SUMMARY.md`
</output>
