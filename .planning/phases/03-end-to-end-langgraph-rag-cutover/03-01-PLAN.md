---
phase: 03-end-to-end-langgraph-rag-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/pyproject.toml
  - src/backend/uv.lock
  - src/backend/agent_search/runtime/graph/state.py
  - src/backend/agent_search/runtime/graph/routes.py
  - src/backend/agent_search/runtime/graph/builder.py
  - src/backend/agent_search/runtime/graph/execution.py
  - src/backend/tests/services/test_agent_service.py
autonomous: true
must_haves:
  truths:
    - "A compiled LangGraph StateGraph exists for the production RAG flow."
    - "The graph explicitly models decomposition, validation/retrieval, sub-answer generation, synthesis, and retry behavior."
    - "Parallel sub-question execution has deterministic merge behavior."
  artifacts:
    - path: "src/backend/agent_search/runtime/graph/state.py"
      provides: "LangGraph state schema and reducer semantics for parallel branches."
    - path: "src/backend/agent_search/runtime/graph/builder.py"
      provides: "Compiled StateGraph wiring from START to END with node and route registration."
    - path: "src/backend/agent_search/runtime/graph/execution.py"
      provides: "Single graph invocation entrypoint with thread-scoped config."
  key_links:
    - from: "src/backend/agent_search/runtime/graph/builder.py"
      to: "src/backend/agent_search/runtime/nodes/"
      via: "StateGraph add_node registrations"
      pattern: "add_node\\("
    - from: "src/backend/agent_search/runtime/graph/routes.py"
      to: "src/backend/agent_search/runtime/graph/state.py"
      via: "Send routing over decomposition sub-questions"
      pattern: "Send\\("
---

<objective>
Create the LangGraph-native runtime foundation required for Phase 3 cutover.

Purpose: Replace ad-hoc orchestration semantics with a compiled graph contract that can be invoked consistently by production runtime entrypoints.
Output: Graph state, routes, builder, and execution modules wired to existing runtime nodes with deterministic fan-out/fan-in behavior.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-end-to-end-langgraph-rag-cutover/03-RESEARCH.md
@src/backend/services/agent_service.py
@src/backend/agent_search/runtime/nodes/
@src/backend/schemas/agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit LangGraph runtime dependencies and graph package skeleton</name>
  <files>src/backend/pyproject.toml, src/backend/uv.lock, src/backend/agent_search/runtime/graph/state.py, src/backend/agent_search/runtime/graph/routes.py, src/backend/agent_search/runtime/graph/builder.py, src/backend/agent_search/runtime/graph/execution.py</files>
  <action>Add explicit direct dependencies for `langgraph` and `langgraph-checkpoint-postgres` in backend dependency declarations (do not rely on transitive availability), then create `runtime/graph` modules for state, route, builder, and execution concerns. Keep existing node logic in `runtime/nodes/*` unchanged so this plan is graph wiring only.</action>
  <verify>docker compose exec backend uv run python -c "from agent_search.runtime.graph.builder import build_runtime_graph; print(callable(build_runtime_graph))"</verify>
  <done>Backend dependency manifest includes LangGraph runtime packages and graph modules import without errors from containerized backend environment.</done>
</task>

<task type="auto">
  <name>Task 2: Implement compiled StateGraph wiring for full RAG lifecycle</name>
  <files>src/backend/agent_search/runtime/graph/state.py, src/backend/agent_search/runtime/graph/routes.py, src/backend/agent_search/runtime/graph/builder.py, src/backend/agent_search/runtime/graph/execution.py</files>
  <action>Implement compiled `StateGraph` with explicit START/END path and named nodes covering decomposition, expand, search, rerank, answer, and synthesize. Use conditional fan-out routing with `Send` for dynamic sub-question lanes. Add reducer semantics in graph state for list/dict merges so parallel lane updates are deterministic and replay-safe. Attach `RetryPolicy` to failure-prone nodes (search/rerank/answer/synthesize) instead of custom retry loops.</action>
  <verify>docker compose exec backend uv run python -c "from agent_search.runtime.graph.execution import execute_runtime_graph; print(callable(execute_runtime_graph))"</verify>
  <done>A compiled graph can be built and invoked through one execution entrypoint and contains explicit node/edge/retry definitions for the full Phase 3 lifecycle.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused tests for graph build contract and deterministic fan-in</name>
  <files>src/backend/tests/services/test_agent_service.py</files>
  <action>Add or update service-level tests to assert graph compile success, route fan-out behavior, and deterministic merge ordering for sub-question artifacts/sub-answers. Keep tests at orchestration boundary and avoid brittle assertions on exact LLM text.</action>
  <verify>docker compose exec backend uv run pytest tests/services/test_agent_service.py -k "graph or langgraph or runner"</verify>
  <done>Tests prove the compiled graph foundation is callable, fan-out/fan-in is deterministic, and no custom thread-pool orchestration assumptions are required at the graph layer.</done>
</task>

</tasks>

<verification>
Run targeted graph foundation tests in container and confirm imports/graph compilation work without local-only assumptions.
</verification>

<success_criteria>
- Graph modules exist and compile in backend container runtime.
- Full lifecycle node sequence is represented in StateGraph wiring.
- Deterministic merge behavior is validated by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-end-to-end-langgraph-rag-cutover/03-end-to-end-langgraph-rag-cutover-01-SUMMARY.md`
</output>
