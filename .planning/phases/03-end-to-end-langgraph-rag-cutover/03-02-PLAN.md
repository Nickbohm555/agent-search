---
phase: 03-end-to-end-langgraph-rag-cutover
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/backend/agent_search/runtime/runner.py
  - src/backend/agent_search/runtime/jobs.py
  - src/backend/agent_search/runtime/__init__.py
  - src/backend/services/agent_service.py
  - src/backend/tests/sdk/test_sdk_run_e2e.py
  - src/backend/tests/sdk/test_sdk_async_e2e.py
autonomous: true
must_haves:
  truths:
    - "Production sync query execution runs through compiled LangGraph runtime only."
    - "Production async query execution also runs through the same compiled LangGraph runtime path."
    - "Legacy imperative orchestration is not required by default runtime path for v1 completion."
  artifacts:
    - path: "src/backend/agent_search/runtime/runner.py"
      provides: "Sync runtime gateway delegating to graph execution layer."
    - path: "src/backend/agent_search/runtime/jobs.py"
      provides: "Async job runtime using compiled graph execution and stage/status updates."
    - path: "src/backend/tests/sdk/test_sdk_run_e2e.py"
      provides: "Regression coverage that SDK sync path routes through runtime graph execution."
  key_links:
    - from: "src/backend/agent_search/public_api.py"
      to: "src/backend/agent_search/runtime/runner.py"
      via: "advanced_rag -> run_runtime_agent"
      pattern: "run_runtime_agent\\("
    - from: "src/backend/agent_search/runtime/jobs.py"
      to: "src/backend/agent_search/runtime/graph/execution.py"
      via: "async job run invocation"
      pattern: "execute_runtime_graph\\("
---

<objective>
Cut over runtime entrypoints so production query orchestration executes only via compiled LangGraph runtime.

Purpose: Enforce SGF-04 on the main runtime path and eliminate hidden fallback to custom imperative orchestration in sync/async execution.
Output: Sync and async runtime gateways both delegate to the graph execution module and tests guard against regression to legacy runner usage.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-end-to-end-langgraph-rag-cutover/03-RESEARCH.md
@src/backend/agent_search/public_api.py
@src/backend/agent_search/runtime/runner.py
@src/backend/agent_search/runtime/jobs.py
@src/backend/services/agent_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace sync runtime runner delegation with compiled graph execution</name>
  <files>src/backend/agent_search/runtime/runner.py, src/backend/agent_search/runtime/__init__.py, src/backend/services/agent_service.py</files>
  <action>Refactor `run_runtime_agent` to use the graph execution entrypoint from Plan 03-01 instead of `services.agent_service.run_parallel_graph_runner`. Keep existing request/response schema contract and Langfuse tracing hooks intact. If legacy helper functions remain, keep them non-production/internal-only and not in default runtime call path.</action>
  <verify>docker compose exec backend uv run pytest tests/sdk/test_sdk_run_e2e.py -k "runtime_runner or sync"</verify>
  <done>Sync SDK/API production path executes via compiled graph runtime and no longer requires `run_parallel_graph_runner` for normal query completion.</done>
</task>

<task type="auto">
  <name>Task 2: Cut over async jobs to shared compiled graph runtime path</name>
  <files>src/backend/agent_search/runtime/jobs.py, src/backend/tests/sdk/test_sdk_async_e2e.py</files>
  <action>Update async job worker execution to invoke the same graph runtime used by sync path. Preserve existing job status payload shape while sourcing stage updates from graph snapshots/events. Ensure cancellation and completion behavior remains compatible with existing API contracts.</action>
  <verify>docker compose exec backend uv run pytest tests/sdk/test_sdk_async_e2e.py -k "async or run_status"</verify>
  <done>Async production run path uses the compiled graph runtime, and job status/cancel contracts remain compatible with existing consumers.</done>
</task>

<task type="auto">
  <name>Task 3: Add cutover regressions that fail on legacy orchestration usage</name>
  <files>src/backend/tests/sdk/test_sdk_run_e2e.py, src/backend/tests/sdk/test_sdk_async_e2e.py</files>
  <action>Add regression assertions/monkeypatch guards that fail if the runtime falls back to `run_parallel_graph_runner` in sync or async entrypoints. Keep tests focused on orchestration path selection and contract parity (not model text determinism).</action>
  <verify>docker compose exec backend uv run pytest tests/sdk/test_sdk_run_e2e.py tests/sdk/test_sdk_async_e2e.py</verify>
  <done>Test suite demonstrates both sync and async runtime paths are LangGraph-native and rejects accidental reintroduction of legacy orchestrator in the main path.</done>
</task>

</tasks>

<verification>
Run SDK-level sync/async tests to verify runtime cutover behavior and contract compatibility under docker-compose backend execution.
</verification>

<success_criteria>
- Main runtime path (sync + async) is backed by compiled graph execution.
- Existing API/SDK response shape remains stable.
- Regression tests enforce no default fallback to legacy orchestrator.
</success_criteria>

<output>
After completion, create `.planning/phases/03-end-to-end-langgraph-rag-cutover/03-end-to-end-langgraph-rag-cutover-02-SUMMARY.md`
</output>
